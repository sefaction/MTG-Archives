# Resumable work checkpoint

## Backup subdirectory correction in progress, 2026-09-26

[PR #411](https://github.com/sefaction/MTG-Archives/pull/411) is open. The grouped local review branch `local/cumulative-review-v11` merged its code with the pending Inventory/Pricing stack and built image `sha256:0e425a60a9dbbd82c11b720258909d7987730a88e4506b2057c65ab3c20b19b2`. Web and both workers run that image; web is healthy and host `/login` returned 200. Local `application`, `pricing`, and `pricing-recovery` directories exist beneath `.local-data/backups`; the read-only package audit found zero packages and failures. With retention disabled for the one command, the actual application backup wrote an 83.9 MB archive to `/app/data/backups/application/mtg-archives-backup-20260926-150543.tar.gz`; `backup:list` showed it and the older August 14 root-level archive. No production application backup was created. #411 needs separate merge approval after final checks and review.

The user updated SMB permissions. The four intended Unraid children now exist, and existing root archives were not moved. A 72,208,405-byte current-schema local Pricing dump was copied to `pricing-recovery-drills/current-schema-20260926-1514/full-dumps/pricing.dump`; source/share/second-readback SHA-256 all matched `16023852f05fe5ef5c778a400efbf27dde45898110ec84c4935f77ac3b505f38`. The read-back dump restored into an isolated local database in 186.6 seconds with 20 public tables and 2,069,447 raw rows; the disposable database was dropped and web remained healthy. Share `packages/restore-evidence.json` also passed read-back hashing. This is a dump drill, not a five-file raw-archive recovery package. No Unraid container or live Pricing data changed. The next #330 gate is a real per-operation package copy/read-back/restore from `pricing-recovery`, followed by intended-host load review. Automatic retention and maintenance stay off.

The new local `application` archive passed the non-destructive restore dry run. The listed August 14 root-level archive failed that dry run with PostgreSQL `unsupported version (1.16) in file header`; older archive visibility does not prove restore compatibility. No forced application restore was attempted. The new archive format remains the operational baseline.

The user clarified that MTG Archives backups belong beneath `\\192.168.1.2\appdata\mtg-archive\backups`, with a child directory per backup type. The Unraid-wide `File Share/appdata backup` directory is the weekly whole-appdata backstop, not the direct Pricing recovery-copy target. Worktree `MTG-Archives-backup-layout` on `fix/pricing-backup-subdirectory-layout` builds on open #403 and changes future application archives to `application` (preserving existing root archives), Pricing source files to the existing `pricing` tree, and per-operation copies to sibling `pricing-recovery`. Pricing recovery validation is being updated to allow disjoint sibling trees beneath one backup root. The user does not require different array or host storage, but a shared root remains one failure domain; weekly whole-appdata backup is later and does not prove a timely copy. Raw deletion and archive maintenance remain disabled.

The initial SMB create attempt returned Access Denied; the user updated the share permissions and the intended children were then created. No real five-file Pricing recovery package has been written or restored. Focused tests and TypeScript pass locally after dependency installation; Compose local and optional recovery overlay configuration resolve. The four previously pending chat questions were not preserved verbatim; reconstruct remaining consequential decisions from live issues/PRs before asking again. The next local Docker reload should include multiple coherent changes.

## Pricing maintenance child deadline, 2026-09-26

Branch `fix/pricing-maintenance-child-timeout` stacks on open, green [PR #398](https://github.com/sefaction/MTG-Archives/pull/398); [PR #403](https://github.com/sefaction/MTG-Archives/pull/403) addresses [issue #402](https://github.com/sefaction/MTG-Archives/issues/402). A child ignoring SIGTERM could hold the optional maintenance runner past its deadline and Central window. The runner now marks timeout/lease interruption as failure, sends SIGTERM, and escalates to SIGKILL after five seconds if the child remains. Recovery cleanup renews the lease while scanning, checks the window again before correction, and reserves one minute beyond the 20-minute child deadline. The direct raw retention pass previously allowed a 65-minute activation with only 55 minutes left; its stage and activation gates now reserve their full child limits and one-minute margin, with bounded child shutdown. Focused timeout/lease/window tests passed on Windows; timeout and lease tests passed inside the Linux application image. Typecheck and diff check passed. The direct-pass follow-up needs fresh Linux checks and CI. This change has not been loaded into the local review services; the running grouped image remains `sha256:84e3d0acf494a2e7a5e044c9c9fb373c6efa7e376fefa5b4e94d16db6a96ed63`. Next safe step: verify the direct-pass follow-up, push, collect CI, then group the application Docker reload with another coherent batch. Raw retention and automated maintenance remain disabled; no PR merge without its individual approval.

## Historical Unraid Pricing restore upgraded and rebuilt, 2026-09-26

Open [PR #398](https://github.com/sefaction/MTG-Archives/pull/398) on branch `docs/pricing-unraid-schema-recovery` stacks on green, unapproved Pricing PR #392 and its parents. Using the supplied SMB account, the September 21 Unraid Pricing cold backup was copied read-only; its 264,741,795-byte tarball matched SHA-256 between the share and local copy, with all 1,367 entries confined to the expected data directory. The restored PostgreSQL 16 instance ran on an internal Docker network without a published port. The exact grouped review image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071` initialized the current Pricing schema once with scheduling/imports disabled: 20 tables and 240 columns match local current schema, while one provider-leading archive-scope index differs. Raw count remained 3,761,388. Full summary rebuild finished 5,707 printings in 175.7 seconds, marked state ready/tier-ready, and preserved raw count/date bounds/price sum. Deterministic 100-scope and 100-month samples had zero mismatches; `pg_amcheck` passed 368 relations/560,200 pages after rebuild. The isolated container, Docker volume and network were removed and the SMB session disconnected; no Unraid data or configuration changed. Automatic approval review rejected deletion of the exact private tar copy despite verified workspace path, so `C:\Users\brian\Projects\MTG-Archives\.local-data\pricing-unraid-restore\pricing-postgres-20260921.tar.gz` remains ignored on disk. Evidence is recorded in #330; both #398 CI jobs passed before this checkpoint update. Next safe step: obtain individual PR approval and verify final-head checks before any merge. This historical archive proves upgrade/rebuild from the Unraid share but not a current-schema time-matched recovery package or intended-host load, so retention/maintenance remain disabled. No PR merge without individual approval.

## Verified cumulative Docker and Inventory move review, 2026-09-26

Branch `perf/pricing-collection-trend-cache` is open as [PR #392](https://github.com/sefaction/MTG-Archives/pull/392), stacked on green, unapproved #391 and #390; both #392 CI jobs are green. The cumulative review branch `local/cumulative-review-v7` at `457b5b4` includes unapproved Inventory #373 and Pricing through #392. Local Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071` was built directly from that worktree; critical source hashes matched the image and running container. After installing test dependencies in the review worktree, `MTG_LOCAL_PILOT_TEST=1` focused Chromium vault pilot passed 1/1 against that image. It selected two rows (10 and 100 copies), chose 5 and 12 before Move, moved 17, and checked stale-selection rollback, occupancy and phone layout. No new app code was changed by this verification. Human Inventory task review and real browser 200% zoom remain open under #265/#274. #373 and #390–#392 each need individual user approval before merge; an approval question for #390–#392 is pending. The separate Pricing recovery destination remains inaccessible with the current Windows SMB credential; current-schema Unraid restore is still outstanding, and retention/maintenance remain disabled. Next safe step: record evidence on #373/#265, continue other authorized issue work, and merge only specifically approved PRs after fresh checks.

## Bounded Collection trend cache under Pricing maintenance load, 2026-09-26

Open [PR #392](https://github.com/sefaction/MTG-Archives/pull/392) stacks on green, unapproved [PR #391](https://github.com/sefaction/MTG-Archives/pull/391), whose parent [PR #390](https://github.com/sefaction/MTG-Archives/pull/390) is also green. A one-worker isolated restore experiment gave Collection p95 6,855 ms versus 7,151 with two workers and lengthened the test; it was reverted before commit. The implemented change caches only Collection trend query results for five minutes and at most 32 entries, keyed by exact filtered holdings, Pricing database and summary generation. State/readiness and Inventory collection reads remain live; failed or stale queries do not persist. Focused Pricing tests 10/10 and typecheck passed. Cumulative review branch `local/cumulative-review-v7` at `457b5b4` was built directly from its worktree as image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`; three critical file hashes matched. Web and both workers were reloaded together, web is healthy, `/login` returned 200, raw retention opt-in is 0. The same four-owner browser test passed from this image: Collection p95 1,690 ms (idle 1,630), Market 770, Data 1,889 and Dashboard 145 across 20 pass-overlap requests per view. Temporary users and databases were removed from all services; live raw count stayed 2,069,447, and optional clone/verifier services were stopped. Next safe step: collect #392 CI, then pursue current-schema Unraid destination recovery and normal-site validation on the intended host under #330. No automatic retention/maintenance or PR merge is authorized without individual approval.

## Four-owner Pricing maintenance load acceptance, 2026-09-26

Open [PR #391](https://github.com/sefaction/MTG-Archives/pull/391) stacks on open, unapproved [PR #390](https://github.com/sefaction/MTG-Archives/pull/390). A local-only browser drill created four temporary signed-in owners with 4,799/1,919/575/119 owned rows and 153,568/15,352/1,725/119 physical copies. It measured warm idle and during-pass requests for Collection, Market, Data and Dashboard, using the named-volume full-size clone and separate verifier. Two runs passed route availability and cleaned up all temporary users and databases; the live Pricing raw count remained 2,069,447 and both optional services were stopped. The second run had 20 requests per view wholly inside the pass: Collection p95 7,151 ms versus 5,033 idle, Market 989 versus 949, Data 2,277 versus 2,019, Dashboard 123 versus 105. Collection still exceeds the existing 5-second warm review target. A read-only local trace found the 90-day trend query about 1.1 seconds and Inventory relation read about 0.8 seconds sequentially; four concurrent traces reached about 4.1 seconds before page rendering. Typecheck and diff check passed. The benchmark script's pass-start marker was copied into the healthy running web container; no grouped Docker reload occurred after #390. Next safe step: collect CI, reduce Collection contention, and repeat the same load check. Current-schema Unraid restore also remains a #330 gate. Retention and maintenance stay disabled; no PR merges without individual approval.

## Named-volume full-size Pricing drill and review provenance, 2026-09-26

Open [PR #390](https://github.com/sefaction/MTG-Archives/pull/390) stacks on green, unapproved [PR #388](https://github.com/sefaction/MTG-Archives/pull/388). Build-context bug [#389](https://github.com/sefaction/MTG-Archives/issues/389) was reproduced: earlier Compose builds with the primary `--project-directory` silently used the primary source worktree, invalidating claims that images `b0a8...` and `ed4b...` contained the full cumulative Pricing stack. A direct Docker build from `local/cumulative-review-v6` at `c49123a` produced image `sha256:8c531533b3558d842bb58103194fdc25e9cc6ed9840831e2bd05724de6f5fe43`; hashes of three critical scripts matched the worktree in both the image and running healthy web container, and `/login` returned 200. #390 adds an explicit local build-context override and an optional named-volume Pricing clone service for the full-size drill. The changed scripts were copied into that running container without a grouped reload. The live Pricing source, clone and verifier had distinct PostgreSQL identities. A 2,069,447-row disposable clone restored in 53.1 seconds and completed a direct one-day retention pass in 99.9 seconds, verifying two copied packages. Source raw count remained 2,069,447. No temporary databases remained on any of the three servers, and both optional services were stopped. Typecheck, Compose profile/context checks and diff check passed. Next safe step: collect PR CI, then group with further batches before the next Docker reload. Normal-site load acceptance and current-schema Unraid recovery remain #330 gates. Automatic retention and maintenance stay off; all open PRs require individual merge approval.

## Complete one-day Pricing rollback fixture, 2026-09-26

Open [PR #388](https://github.com/sefaction/MTG-Archives/pull/388) stacks on green, unapproved [PR #387](https://github.com/sefaction/MTG-Archives/pull/387). The disposable retention fixture optionally replaces its source database from the copied pre-compaction dump after the full one-day pass, with the original backup directory hidden. Two local Docker runs passed with the separate verifier and both raw-activation and full-pass rollback flags: two original raw rows and two daily rows returned, both retention boundaries were unset, the source revision matched, and no archive segment remained. Typecheck, diff check and both PR CI jobs passed. No live Pricing row changed. The earlier claim that image `ed4b...` represented cumulative review code was incorrect; see the provenance correction above. Actual Unraid destination restore and normal-site load acceptance remain under #330. Each PR needs individual merge approval.

## Daily pre-compaction recovery package, 2026-09-26

Open [PR #387](https://github.com/sefaction/MTG-Archives/pull/387) stacks on green, unapproved [PR #386](https://github.com/sefaction/MTG-Archives/pull/386). Daily compaction now copies its verified pre-delete full dump, manifest and package index to `PRICING_RECOVERY_COPY_DIR` and reads them back before deleting old daily rows. A local-only restore drill verifies the copied package without the source backup directory and restores it to a disposable verifier database. Typecheck, focused tests 5/5 and the small direct Docker retention fixture passed with the backup source temporarily hidden during copied daily restore; the existing raw activation rollback check also passed. The disposable databases/files were cleaned up, the verifier was stopped after testing, and the local app remains on cumulative image `sha256:b0a8b7760087cbe2db88db4220f9fb1f0513684e18e3e6e527fd6721b04e05ee`; only changed scripts were copied into the running web container for testing. No live raw or daily row changed. Next safe step: collect CI and continue #330 with actual Unraid destination recovery. Automatic retention and archive maintenance remain disabled. Do not merge without individual PR approval.

## Pricing raw-activation rollback drill, 2026-09-26

Active branch `test/pricing-retention-rollback-drill` is open [PR #386](https://github.com/sefaction/MTG-Archives/pull/386), stacked on green, unapproved [PR #385](https://github.com/sefaction/MTG-Archives/pull/385). The small local fixture now optionally restores the copied pre-activation dump into its original disposable database after a verified raw activation. With `MTG_LOCAL_PILOT_TEST=1`, `PRICING_VERIFY_POSTGRES_DRILL=1`, `PRICING_DIRECT_VERIFICATION_DRILL=1` and `PRICING_ROLLBACK_DRILL=1`, two local Docker runs passed: copied-package restore validation, database replacement, two recovered raw rows, old archive boundary, matching daily boundary and source revision, and no active raw segment. Both Pricing servers were checked for leftover disposable databases (none); the optional verifier was stopped afterward. This is test-only code copied into the healthy running web container, so no grouped Docker reload was needed. It proves raw-activation rollback from the copied package on disposable data, not rollback of prior daily compaction or actual Unraid/live replacement. Typecheck, diff check and both PR CI jobs passed. Next safe step: continue #330 full operating recovery and controlled load acceptance while #386 awaits individual approval and its Pricing parents merge in order.

The cumulative review branch now includes the measurement commits and isolated-verifier batch. One grouped image `sha256:b0a8b7760087cbe2db88db4220f9fb1f0513684e18e3e6e527fd6721b04e05ee` was built and loaded into web, Pricing worker and notification worker. Web is healthy, `/login` returned HTTP 200, and the small isolated-verifier fixture passed again from that exact image. The worker retention opt-in is `0`; the optional verifier container was stopped after testing, and archive maintenance is not running. [PR #385](https://github.com/sefaction/MTG-Archives/pull/385) is green and open for individual approval.

## Optional isolated Pricing verifier pilot, 2026-09-26

Active branch `feat/pricing-isolated-verifier` stacks on green, unapproved [PR #384](https://github.com/sefaction/MTG-Archives/pull/384). It adds a separate `pricing-verify-postgres` service only in the disabled maintenance profile, on a Docker named volume, and routes temporary daily compaction, raw stage/activation and archived-correction verification databases there when `PRICING_VERIFY_DATABASE_URL` is set. The helper requires that service hostname and checks different PostgreSQL system identifiers. Enabled archive maintenance refuses a missing verifier URL; direct local pilots retain their fallback. Typecheck, Compose default/profile service checks, focused recovery/preflight tests 4/4 and the small disposable direct retention fixture passed with the separate verifier. A full-size direct drill then passed on 2,069,447 source-clone raw rows: the one-day pass took 202.9 seconds (compaction 120.0, stage 1.0, activation 82.0), versus 663.3 seconds on the same-server path. Eight anonymous Public Inventory requests during isolated verification had p95 3.86 seconds, versus 2.31 seconds in eight post-pass requests; this still needs a controlled latency budget. Three prior attempts stopped before a complete isolated pass: insufficient window after clone restore, one source-clone file-allocation interruption and a verifier crash on its former Windows bind mount. The crash left a 225 MB disposable verifier database; it was explicitly removed before switching to the named volume. The successful run left no temporary databases on either server. No live raw data changed; archive maintenance and retention remain disabled. Next safe step: commit this batch, merge into cumulative review for one grouped Docker build/reload, open dependent PR and collect CI. #330 remains open for normal-site concurrency, Unraid current-schema restore and rollback gates; each PR needs individual approval before merge.

## Pricing retention phase timing, 2026-09-26

Active branch `feat/pricing-retention-phase-timing` stacks on green, unapproved [PR #383](https://github.com/sefaction/MTG-Archives/pull/383). The full-size disposable Pricing clone drill passed again on 2,069,447 raw rows. Its one-day pass took 663.3 seconds: daily compaction 358.8 seconds, raw staging 2.9 seconds, guarded activation 297.0 seconds. Dump/restore of the source clone took 50.2/233.6 seconds outside that pass. The two full database verification phases dominate load; a four-request Public Inventory sample during clone restore took 2.4–5.2 seconds. Typecheck and diff check passed; the timed script was copied into the running local container for this drill, so no grouped Docker reload occurred. The test exited 0 and its finally cleanup dropped the clone/files. The web remained healthy, raw retention and archive maintenance remain off, and no live raw row was changed. Next safe step: open dependent PR, then design and test verification-load isolation or limits and a current-schema restore from the actual Unraid destination before #330 activation. Every PR still requires individual approval before merge.

## Full-size raw-retention load drill, 2026-09-26

Branch `feat/pricing-retention-fullsize-drill` stacks on green, unapproved [PR #382](https://github.com/sefaction/MTG-Archives/pull/382); [PR #381](https://github.com/sefaction/MTG-Archives/pull/381) is its green, unapproved parent. The local-only drill cloned 2,069,447 live raw rows into a disposable 2.27 GB Pricing database, injected one 120-day-old row in the clone, and completed the new one-date retention pass and copied-package audit. Dump: 42.9 seconds, restore: 202.5 seconds, pass: 664.2 seconds, package: 72.1 MB. Small Public Inventory request samples were p95 5.5 seconds during the pass and 2.0 seconds afterward. The test exited successfully and its `finally` cleanup dropped the clone and temp files; live raw data was not changed. The running cumulative local Docker image already has #382 application code; this new branch adds only the reproducible drill and evidence and needs no reload. Next safe step: typecheck and review, open its dependent PR, then work on the measured load/rollback and actual Unraid destination restore gates under #330. All Pricing raw retention and maintenance opt-ins remain off; each PR needs individual approval before merge.

## Bounded off-hours raw retention pilot, 2026-09-26

Active branch `feat/pricing-bounded-retention-pass` stacks on unpushed `feat/pricing-recovery-partial-cleanup`, after open #380. It adds a read-only oldest-date plan and a separately opted-in one-date raw retention pass inside the existing 2–5 a.m. Central lease. With all three local pilot flags, the importer pauses new jobs during that window and the pass refuses active jobs, performs required daily compaction, verified staging and guarded activation, and has an 85-minute parent limit. The new flag defaults to 0 in Compose and the maintenance profile remains disabled. Typecheck, focused gates 5/5 and Compose opt-out configuration passed. Cumulative Docker image `sha256:e82afb93d7d290c6a79d491f356f19620bcf804f64546b67c7e10a16e3a1dd12` is healthy; both workers run, `/login` returned HTTP 200, and the read-only live plan found no eligible raw date (`oldest=2026-06-30`, `cutoff=2026-06-27`). A disposable PostgreSQL fixture exercised daily compaction, stage, activation and copied-package audit for one 2026-05-29 row in 15.3 seconds; one recent row remained and test database/files were removed. No live raw row was deleted. Next safe step: push the two dependent batches and open separate PRs, collect CI, then continue #330's independent current-schema restore and resource/rollback gates. Each PR requires its own merge approval.

## Pricing interrupted-copy cleanup review, 2026-09-26

Active branch `feat/pricing-recovery-partial-cleanup` stacks on open, green #380 (after #379 and #377). During an explicitly enabled 2–5 a.m. Central maintenance pass, the runner now scans the separate recovery destination once under its lease and removes only its own unpublished `.partial-UUID` copies older than seven days. No completed package or recent copy is eligible. The focused recovery-copy tests passed 3/3 and typecheck passed after Prisma generation. The maintenance profile remains disabled. Next safe step: review final diff, merge cumulatively for the next grouped Docker reload, open a dependent PR and collect CI. #330 remains open for current-schema independent restore, resource and rollback gates; no raw deletion has been enabled.

## Recovery mount and Unraid evidence review, 2026-09-26

Open dependent [PR #379](https://github.com/sefaction/MTG-Archives/pull/379) adds the optional Pricing recovery destination mount, and [PR #380](https://github.com/sefaction/MTG-Archives/pull/380) records the historical Unraid cold-backup restore. The cumulative local review branch includes both commits plus the earlier open Pricing and Inventory stack. Its web uses image `sha256:74b732ae1f14546befc15afdb441fbeac9181ac1bb7e648c02502c95eede0007`, is healthy, has the local recovery bind, and returned `/login` HTTP 200; both workers run. The read-only recovery package audit found zero packages and zero failures. Raw deletion and the optional maintenance profile remain disabled. Both PRs await checks and individual merge approval. The user confirmed the already-implemented #373 behavior: full-stack default with an up/down amount control on each selected row before Move. The extracted historical backup under `.local-data/unraid-pricing-restore-20260921` remains because automatic approval review rejected recursive deletion; its disposable container and SMB mapping are gone. Next safe step: check PR CI and continue #330 recovery verification without changing production Unraid configuration; obtain explicit approval before any merge.

## Unraid historical Pricing cold-backup restore, 2026-09-26

The existing Unraid `File Share/appdata backup/ab_20260921_000001/mtg-archives-pricing-postgres-1.tar.gz` was read over SMB using the supplied account; no secret was stored. The plugin log shows PostgreSQL stopped, backup verified and restarted. Its 264,741,795-byte tarball had SHA-256 `c97e9c7caa278d49c2cc75393b4d5dd87f2e00f529f13fdce6204054109e088b`; 1,339 regular files and 28 directories had safe expected paths. A local extracted copy started PostgreSQL 16 cleanly in a no-network disposable container: 3,761,388 raw rows, 2026-07-03 through 2026-09-20, price sum 40,678,185.9600. `pg_amcheck` passed; the test container and SMB mapping were removed. The local extracted data remains at `C:\Users\brian\Projects\MTG-Archives\.local-data\unraid-pricing-restore-20260921` because automatic approval review rejected recursive deletion despite verified workspace path. The backup predates current summary/archive schema, is same-host and weekly, and does not satisfy the timely recovery-copy gate. A newer current-schema backup and copied-package restore from Unraid remain #330 work. No production data or Unraid configuration changed.

## Opt-in Pricing recovery destination mount, 2026-09-26

Active branch `feat/pricing-recovery-mount` stacks on green, unapproved PR #377. The local Compose overlay binds a separate `.local-data/pricing-recovery` directory into web and optional maintenance, still on the same laptop disk. A new operator override requires an explicit existing host mount path and refuses to create a missing one; neither starts the disabled maintenance profile. Compose configuration verified the mount/env and disabled profile, and one-off web/maintenance containers each ran the read-only package audit with zero packages. The actual Unraid destination is not mounted or drilled. Next safe step: commit this batch, prepare a second coherent #330 batch, then one cumulative Docker reload and separate dependent PRs. No raw deletion or production infra change. #330 remains open; individual PR approval is required for merge.

## Copied Pricing package audit, 2026-09-26

Active branch `feat/pricing-recovery-package-audit` stacks on `feat/pricing-recovery-package`, which stacks on green, unapproved PR #375. This adds a read-only source-independent checksum audit of all published recovery package indexes and their four files. The shared verifier is used by the package restore drill. Isolated PostgreSQL fixture passed healthy activation/correction packages, a tampered manifest audit failure and the source-loss restore case. Typecheck and `git diff --check` passed. Next safe step: commit, merge both new batches into cumulative local review, build/reload Docker once, push and open separate dependent PRs, then collect CI. The selected Unraid destination is still not mounted or drilled; raw deletion and optional maintenance remain disabled. #330 stays open and each PR needs individual merge approval.

## Source-independent Pricing recovery package, 2026-09-26

Active branch `feat/pricing-recovery-package` stacks on green, unapproved PR #375 (itself after #374 and #372). Archive activation and older-correction pilots now prepare an immutable relative-path index and copy/read back five files before changing live Pricing. The local restore drill accepts the copied package path without the source `BACKUP_DIR` or receipt. The isolated PostgreSQL fixture restored both operation packages and passed archive activation with the entire source directory temporarily unavailable; typecheck passed. The full isolated PostgreSQL fixture passed again after package-mode tamper rejection, and git diff --check passed. Next safe step: commit, load cumulatively into local Docker, push dependent PR and collect CI. No Unraid target is mounted or drilled, no off-host recovery is proven, and raw deletion/maintenance remain disabled. #330 stays open. No PR merge without its individual approval.

## Copied Pricing recovery restore drill, 2026-09-26

Active branch `feat/pricing-recovery-copy-restore` is open [PR #375](https://github.com/sefaction/MTG-Archives/pull/375), stacked on open, unapproved #374, which depends on open, unapproved #372. This batch copies the stage/replacement and pre-operation backup manifests along with the raw archive and full Pricing dump. A local opt-in drill checks all four destination hashes, the archived CSV hash, and restores the copied dump into a disposable Pricing database to compare pre-operation raw counts, price sum and source revision. The isolated PostgreSQL fixture passed for both activation and older correction, rejected a tampered copied manifest, and cleaned up its test database. Typecheck, `git diff --check`, production build and cumulative local Docker reload passed; image `sha256:5efe3c1304f656140b0058ce858adef554c1f7101672be30fc66cea0776cc1f8` runs on healthy web and both workers, and `/login` returned HTTP 200. Next safe step: collect PR CI and continue independent #330 work. The Unraid target is not mounted, no actual source-directory-loss restore has run, no off-host copy is proven, and automatic raw deletion and maintenance remain disabled. #330 stays open. Do not merge without individual PR-number approval.

## Pricing maintenance recovery-copy preflight, 2026-09-26

Active branch `feat/pricing-maintenance-copy-gate` stacks on green, open [PR #372](https://github.com/sefaction/MTG-Archives/pull/372). This batch requires an existing separate `PRICING_RECOVERY_COPY_DIR` before enabled archived-correction maintenance starts or claims its lease; the Compose service passes that env value, but no production destination is mounted or enabled. The read-only backlog command remains available without it. Recovery-copy unit and missing-target runner tests passed, as did typecheck and Compose configuration with the local env file. Next safe step: open dependent PR and collect CI. Raw deletion and the optional maintenance profile remain disabled; #330 stays open. Do not merge either PR without individual approval.

## Grouped Inventory copy stepper review, 2026-09-26

Open, unapproved [PR #409](https://github.com/sefaction/MTG-Archives/pull/409) now includes visible per-row decrease/increase buttons in Table and Binder views as well as invalid-selection feedback. Local branch `local/cumulative-review-v11` at `38e3fd9` combines it with the prior v10 Inventory/Deck/Pricing review stack; this is a local review merge only. A direct Docker build produced image `sha256:56de6036d2b72f91f86fb95b867fdc77189d318d3eb5287fd77fb9299b8dbcf4`, running on web and both workers, with healthy web and host `/login` HTTP 200. The owned phone touch case passed 1/1 at 390px and 320px: full-stack defaults, invalid range feedback, visible stepper buttons, and moves of two and one chosen copies. Fixture users returned to zero and the snapshot remains 12,477 physical copies. Typecheck and production build/client manifest checks passed. Final head CI and human Inventory review remain pending; no PR merge without individual approval. Pricing retention and maintenance remain disabled.

## Invalid Inventory selected-copy feedback, 2026-09-26

Branch `fix/inventory-copy-quantity-feedback` is open [PR #409](https://github.com/sefaction/MTG-Archives/pull/409), stacked on green, unapproved [PR #406](https://github.com/sefaction/MTG-Archives/pull/406). The 390px owned touch fixture reproduced [issue #408](https://github.com/sefaction/MTG-Archives/issues/408): setting the selected-copy input to 0 left it without `aria-invalid` and no visible row error. A shared table/card selected-copy control now shows a 1–stack-size range error, links it through `aria-describedby`, and marks invalid state. The Move dialog explains why confirmation is disabled and how to correct the row. The user confirmed that every selected row should default to its full stack and have visible controls for choosing fewer before Move; the shared control now has explicit decrease/increase buttons beside the numeric input. The expanded fixture covers invalid amounts at 390px and 320px, uses both buttons to correct the amount, then commits the chosen moves. The final grouped Docker browser review passed; see the current entry above. No PR merge without individual approval.

## Inventory touch move and singular copy wording, 2026-09-26

Branch `test/inventory-touch-copy-move` stacks on green, open [PR #404](https://github.com/sefaction/MTG-Archives/pull/404) and its unapproved Inventory parents. The expanded owned touch fixture passed at 390px and 320px on the prior grouped image when its existing plural wording was accepted: it tapped an Inventory row, reduced its copy count before Move, selected a destination and committed moves of two and one copies. Zero fixture users and 12,477 physical copies remained. The one-copy path exposed [issue #405](https://github.com/sefaction/MTG-Archives/issues/405); the code now renders singular selection, review, confirmation and success labels, and the final test requires them. Typecheck and final Docker review/CI are pending. This application change is not in the currently running grouped image `sha256:a8f4118dd9988b8c3c66f1562e75e01f7a92f071c2139f84477de2cc3d0aac81`. Next safe step: commit/open a dependent PR, then combine it with another batch for one Docker reload and rerun the final touch fixture. No PR merge without individual approval.

## Locations editing at actual browser zoom, 2026-09-26

Branch `test/locations-real-browser-zoom` stacks on open, green Inventory [PR #401](https://github.com/sefaction/MTG-Archives/pull/401) and its unapproved parents. The existing owned Locations workspace fixture now opens its selected vault in a disposable actual 200% Chrome tab zoom profile, verifies a 683×384 CSS viewport at device pixel ratio 2, avoids page-level horizontal overflow, and uses keyboard Enter to open Manage and save a description with the Save button in viewport. The focused fixture passed 1/1 against the current grouped Docker image `sha256:84e3d0acf494a2e7a5e044c9c9fb373c6efa7e376fefa5b4e94d16db6a96ed63`; typecheck passed, fixture users returned to zero and physical copies remained 12,477. Headless zoom screenshot capture was blank, so no visual screenshot claim is made. Test/docs only; no Docker reload needed. Next safe step: commit, push, open dependent PR, collect CI and continue #274 acceptance. No merge without individual approval.

## Inventory row control names for same-name printings, 2026-09-26

Open [PR #401](https://github.com/sefaction/MTG-Archives/pull/401) on branch `fix/inventory-row-control-labels` stacks on green, unapproved Inventory stepper PR #399 and its parents; discovered bug [#400](https://github.com/sefaction/MTG-Archives/issues/400) is open. Exact Inventory rows can share a card name while differing by printing, finish, condition, language or owner. Both table/card checkboxes and selected-copy controls now include those attributes in their accessible names; one-entry summaries use singular grammar. A grouped local Docker build from `local/cumulative-review-v8` at `a57639e` produced image `sha256:84e3d0acf494a2e7a5e044c9c9fb373c6efa7e376fefa5b4e94d16db6a96ed63`. Pricing worker and summary SQL hashes matched the source/image, and compiled Inventory code contains the new control label; web, Pricing worker and notification worker run that image, with `/login` HTTP 200. Focused browser cases for duplicate Forest labels, storage layout, vault map and vault pilot passed 4/4; temporary users cleaned to zero and physical Inventory stayed 12,477 copies. Typecheck and production build/client manifest guards passed. Next safe step: collect #401 CI and human Inventory review. Raw Pricing retention/maintenance remain disabled; no merge without individual PR approval.

## Selected Inventory copy stepper keyboard check, 2026-09-26

Open [PR #399](https://github.com/sefaction/MTG-Archives/pull/399) on branch `test/inventory-copy-stepper-keyboard` stacks on green, unapproved regression PR #397 and its Inventory/Deck/navigation parents. The local app remains on provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`; this test/docs-only batch needs no reload. At actual 200% Chrome tab zoom the Inventory case confirmed two selected rows default to full 8 and 17 copies, ArrowDown/ArrowUp changes only the focused 8-copy control and restores it, the control fits the 683×384 CSS viewport after scrolling, and the existing independent 5+12-copy Move succeeds. A whole-row element screenshot stalled under browser zoom, so it was removed; the final assertions passed 1/1 in 12.3 seconds. Typecheck/diff check passed and fixture users cleaned to zero. Next safe step: collect #399 CI and seek its individual merge approval. Human Inventory review and broader #274 matrix remain open; no PR merge without individual approval.

## Full local browser regression and quantity choice, 2026-09-26

Open [PR #397](https://github.com/sefaction/MTG-Archives/pull/397) on branch `test/inventory-move-regression-alignment` stacks on green, unapproved navigation PR #396 (and its Deck/Inventory parents). The user reaffirmed that selecting an Inventory row should choose its full stack, with a row-level up/down quantity control to reduce that amount before Move; green, unapproved PR #373 already implements this behavior. The full 61-case Chromium suite on the provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071` recorded 57 passes, one skip and three failures: two older storage/vault fixtures looked for removed quantity controls inside Move, and the Imports theme assertion sampled a link while it was hovered. This test-only branch selects 15 and 10 copies beside rows, verifies those amounts in Move (including after Cancel), and clears hover before comparing the Imports resting theme color. All three affected browser cases passed on rerun, 3/3; typecheck and diff check passed. #274 has an evidence comment. Next safe step: collect #397 CI and request individual approval when green. No app source change or Docker reload is needed. Human Inventory, Deck and navigation feedback and individual PR approvals remain open; do not merge without each explicit approval.

## Navigation across sections at actual browser zoom, 2026-09-26

Open [PR #396](https://github.com/sefaction/MTG-Archives/pull/396) stacks on open, unapproved Deck PR #395 and its Inventory acceptance parents. The local Chrome 200% zoom setup was extracted into `tests/ui/local-browser-zoom.ts` and shared by Inventory, Deck and navigation fixtures. On the provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`, all four affected browser cases passed after the helper refactor; the focused navigation case passed again after keyboard Tab coverage. At 683×384 CSS px, the section menu scrolls internally, Decks is keyboard reachable, Escape returns focus, and Decks/Settings/Locations/Inventory navigation preserves current-route state without page overflow. The menu screenshot was inspected; typecheck passed. This test/documentation-only batch needs no Docker reload. Next safe step: collect CI and continue the remaining #263/#274 acceptance. Human navigation review remains open; no merge without individual approval.

## Deck editing at actual browser zoom, 2026-09-26

Open [PR #395](https://github.com/sefaction/MTG-Archives/pull/395) stacks on green, unapproved Inventory PR #394 to reuse its test-only local Chrome zoom fixture; #394 depends on #393/#378/#373. On the provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`, the existing Deck builder browser cases passed 2/2, then passed 2/2 again with an actual 200% zoom edit path. The 1366×768 profile reported a 683×384 CSS viewport at device pixel ratio 2. Add, Selection and Deck options dialogs fit the viewport, Escape returned focus, Add increased the deck list from 101 to 102 without physical Inventory mutation, and Analysis returned to the same builder. The zoomed Add screenshot was inspected; typecheck passed. This is a test/documentation-only batch, so no Docker reload is needed. Next safe step: collect CI and continue #268/#274 acceptance. Human Deck review remains open; no merge without individual approval.

## Actual Inventory browser zoom review, 2026-09-26

Open [PR #394](https://github.com/sefaction/MTG-Archives/pull/394) stacks on open, unapproved Inventory #393 (which depends on #378 and #373). A test-only local extension in a disposable Chromium profile uses Chrome `tabs.setZoom(2)` on the local Inventory tab. The opt-in Inventory workspace browser case passed 1/1 on the provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`: a 1366×768 browser viewport became a 683×384 CSS viewport at device pixel ratio 2, with visible table, no page overflow, reachable modal Close/Apply, Escape focus return and invalid-query feedback. It selected 5 of 8 and 12 of 17 copies before Move, chose a Box, kept the Move dialog within the zoomed viewport and completed a 17-copy move. Both 200% screenshots were visually inspected. Typecheck passed. This test/documentation batch changes no app source and needs no Docker reload. Automatic approval review rejected recursive cleanup of an ignored proof-of-concept profile; that local scratch folder remains untouched. Next safe step: collect CI, then continue the remaining #274 tasks and request individual PR approval. Human Inventory task observation and broader cross-workflow zoom/accessibility remain open; no merge without individual approval.

## Inventory selected-copy contrast follow-up, 2026-09-26

Branch `fix/inventory-selection-contrast` stacks on green, unapproved Inventory PR #378 and #373. On the provenance-checked grouped Docker image `sha256:dc9fe2b8afc8810b9d161f17ac1778ff8cc9226d3010f673977866565a5f3071`, the opt-in vault browser case passed 1/1 after adding all-six-theme computed contrast checks for the selected-copy toolbar. The lowest measured Actions label ratio was 5.82:1 and chosen-copy count 14.74:1. Typecheck passed after generating the local Prisma client. Existing workspace CSS already remaps gray utility classes to theme tokens, so no app source or Docker reload is needed for this test/documentation batch. Next safe step: review diff, push dependent PR, collect CI. Human Inventory review and actual browser 200% zoom remain under #265/#274; no PR merge without its individual approval.

## Enlarged Inventory filter review, 2026-09-26

Active branch `test/inventory-enlarged-view` stacks on green, unapproved PR #373. The current cumulative local Docker image includes #373; this batch changes only the Inventory browser fixture and acceptance ledger, so no app rebuild is needed. Chromium rendered and captured 683×384 CSS px at device pixel ratio 2, equivalent to the layout area of a 1366×768 window at 200% zoom. The Inventory workspace case passed with visible results, no page-level overflow, modal Filters, reachable Close/Apply, Escape focus return and invalid-query feedback. The captured 683×384 image was inspected. This is emulation; actual browser-chrome zoom and human task observations remain open under #274. Next safe step: commit, push, open dependent test PR and collect CI. No PR merge without individual approval.

## Inventory copy amounts before Move, 2026-09-26

Active branch `fix/inventory-selected-copy-clarity` from merged main. The user chose full-stack selection by default with an up/down control beside each selected Inventory row to choose fewer physical copies before Move. This branch carries the chosen per-row amounts through the move request, validates them against current stack quantities in one transaction, and supports multiple partial splits with audit and reservation checks. The all-matching shortcut retains its aggregate limit. Local Docker uses image `sha256:fa73bb6ceac9c957fd2c4c50f826c3a78a7e649c972e64fd0d594409200845da`; web is healthy and `/login` returned HTTP 200. Focused vault browser coverage passed 1/1, including chosen partial amount, cancellation, stale-selection failure, occupancy and phone layout. Storage planner tests 4/4 and typecheck passed. Next safe step: review final diff, open PR, collect CI and human Inventory task feedback; #265 remains open. Do not merge without individual approval. Independent Pricing recovery copy PR #372 is green and unapproved.

Follow-up: the stronger two-partial browser fixture showed that one visible Exact-printing row can include several underlying storage records. The request now carries visible row groups and chosen totals, and the server allocates each total across that group's movable records oldest first. Cumulative local image `sha256:dfac076c44282613485cbb1495570c1fdb67a52914fcbf12b5fbbf6b4c2e80aa` includes review PRs #372/#373 plus the dependent Pricing maintenance gate branch. Web is healthy with `/login` HTTP 200. The expanded vault browser case passed 1/1 with two simultaneously split source stacks, stale-selection rollback, conserved copies, refreshed occupancy and phone layout. The Pricing maintenance profile and raw deletion remain disabled. PR #373 CI is rerunning after this correction; individual merge approval remains required.

## Verified Pricing recovery copy pilot, 2026-09-26

Active branch `feat/pricing-verified-recovery-copy` from merged main. Optional `PRICING_RECOVERY_COPY_DIR` copies and reads back the immutable raw archive and full Pricing database dump before guarded local archive activation or archived correction apply. An unavailable copy destination stops activation before live raw deletion. The isolated PostgreSQL archive/correction fixture, recovery-copy unit case, and typecheck passed. No Unraid mount is configured for this variable, no recurring maintenance or raw deletion is enabled, and same-host copy is not off-host recovery proof. Open a review PR and keep #330 open; the Inventory quantity-selection branch is separate. Do not merge without individual approval.

## Inventory acceptance guide integration, 2026-09-26

The user individually approved PR #371. Approved backup documentation PR #370 merged to main with fresh green checks. #371 updates the Inventory-first local review guide and acceptance status, while preserving #370 knowledge in main; only this historical checkpoint conflicted. Fresh Core and PostgreSQL checks are required before #371 merges. Cumulative Docker rebuild from exact merged application source is in progress. UI human review and browser 200% zoom remain open; Pricing raw deletion and optional maintenance remain disabled.

## Inventory-first review instructions, 2026-09-26

Active branch docs/inventory-review-status from merged main after approved #369. The user chose Inventory filtering and moves first. The cumulative healthy Docker image includes Pricing through #368 and Inventory #369; focused workspace/navigation and color-filter/vault-move browser cases passed 5/5. LOCAL_REVIEW_BUILD.md and UI_ACCEPTANCE_STATUS.md now give the current task sequence and limits. Actual browser 200% zoom and human task observations remain open. This documentation branch needs PR/CI and individual approval. Separate #370 documents observed Unraid backup coverage and remains unapproved. Next safe step: open this PR, collect human findings, and continue #330 copy/recovery gates without enabling raw deletion or maintenance.

## Archived no-change feed main integration, 2026-09-26

The user explicitly approved PR #368. Approved #365 and #366 have merged to main with fresh green checks, and #368 now targets current main. Its code and fixture merged automatically; this historical checkpoint is the only conflict. The isolated PostgreSQL fixture passed terminal sparse-source settlement, preserved summaries/archive, and a later complete correction. Fresh Core and PostgreSQL checks are required before merge. Raw deletion and optional Pricing maintenance remain disabled. Existing Unraid Appdata Backup coverage is known, but off-host copy and restore from that share remain unproved.

## Pricing no-change feed settlement, 2026-09-26

Active branch fix/pricing-archived-noop-queue builds on unapproved #366. Approved #356 merged into main; approved #357-#359 and #362-#364 merged into their parent feature branches, so green rollup PR #367 now targets main and requires its own approval. Approved #361 merged into main after a checkpoint conflict resolution and fresh green checks; #360 closed. This batch settles zero-change archived feeds, records missing-identity source anomalies as terminal, and preserves archive/summary state. A later full feed still applies. Typecheck and the isolated PostgreSQL fixture passed. The optional Pricing maintenance profile and raw deletion remain disabled; no independent Unraid copy proof exists. The user chose the existing Unraid backup setup, but its job, destination and read-back method are undocumented and a focused question is pending. Inventory filtering/moves is first for UI review; local navigation branch fix/navigation-narrow-desktop-fold is committed but not yet loaded/tested. Next safe step: commit and open the no-op correction PR, then build a cumulative local Docker image with the Inventory branch and run its focused UI checks. PR #367 and all other new PRs require individual approval before merge.

## Full-size local Pricing restore drill, 2026-09-26

Active branch `feat/pricing-full-restore-drill` is open, unapproved [PR #366](https://github.com/sefaction/MTG-Archives/pull/366), stacked on green, unapproved [PR #365](https://github.com/sefaction/MTG-Archives/pull/365). New `scripts/pricing-full-restore-drill.ts` is a local-opt-in, repeatable full Pricing dump/isolated restore/parity/cleanup command. With the Pricing importer paused, it passed on the 2,069,447-row local snapshot: 72,030,818-byte dump, 40.8-second dump, 181.6-second restore, 2,270,411,799-byte restored database, matching 13 table counts/price totals and complete summary state before/after. SHA-256 remained stable. The disposable database and verified dump were removed; importer restarted and is running. Web is healthy; read-only planner still shows 2,069,447 raw rows, zero old candidates, fresh summaries and deletion disabled. During restore, `/login` p95 was 80 ms over 20 sequential requests, Public Inventory p95 was 2,175 ms over 12, versus 1,847 ms for 12 idle requests afterward. These are small local availability samples, not an authenticated Pricing load budget. Typecheck passed; #366 CI pending. The running image remains `sha256:c262db5712b96beaa36ee9da9029ee4ae2dc95b05c4afc70f5041f47e8f2a982` through #364 plus #361. No independent service copy or normal raw deletion. Next safe step: verify CI, then perform the next cumulative reload with #365/#366. Backup destination/verification and UI workflow questions remain pending with the user; no PR merge approval has been received.

## Archived Pricing recovery audit in progress, 2026-09-25

Active branch `feat/pricing-archive-recovery-audit` is open, unapproved [PR #365](https://github.com/sefaction/MTG-Archives/pull/365), stacked on green, unapproved [PR #364](https://github.com/sefaction/MTG-Archives/pull/364). A new read-only command verifies active raw archive gzip and decompressed CSV hashes, full Pricing backup hashes and pending feed spool hashes against Pricing metadata, while rejecting missing, non-file or out-of-root paths. It explicitly reports that independent copy and restore proof are separate. The disposable PostgreSQL fixture now restores the pre-correction backup into a separate database after applying an old correction and checks original archive generation, queue, visible price and revisions. It also confirms a tampered active archive causes a nonzero audit result. Typecheck, fixture and `git diff --check` passed; #365 CI is pending. Local snapshot audit has no active segments or pending feeds and reported healthy, with independent copy and restore unverified. No normal raw row was deleted; the review Docker image remains `sha256:c262db5712b96beaa36ee9da9029ee4ae2dc95b05c4afc70f5041f47e8f2a982`. Next safe step: verify CI, then configure existing backup service target when the user identifies its path and verification method. Do not enable maintenance or merge any unapproved PR.

## Disabled archived Pricing maintenance runner in progress, 2026-09-25

Active branch `feat/pricing-archive-maintenance-runner` is open, unapproved [PR #364](https://github.com/sefaction/MTG-Archives/pull/364), stacked on green, unapproved [PR #363](https://github.com/sefaction/MTG-Archives/pull/363). The optional Compose profile is disabled by default and requires explicit local opt-ins for apply. The runner checks the DST-aware 2-5 a.m. Central window, reports backlog, claims a single expiring Pricing DB lease, renews it while one bounded correction child runs, records attempts/errors and retries after 15 minutes. It leaves later batches pending. Typecheck, Central winter/summer/DST test and Compose config validation passed; a read-only `--once` against the local snapshot reported zero pending queues. The disposable PostgreSQL fixture passed 501-identity continuation and live-lease exclusion/expired-lease takeover. #364 CI is pending. No enabled schedule, normal raw deletion or external backup copy exists. Current review Docker image remains `sha256:808efb6341d5da559377f0fc5b8a111956b41f2c8b9209a656009bae669c148c` (earlier cumulative #356-#363 plus #361); reload after a few batches. Next safe step: verify #364 CI, then pursue independent service copy proof and recovery/load gates before activation. User's exact existing backup service/path/read-back method is still pending clarification.

## Bounded queued Pricing correction revision, 2026-09-25

Active branch `feat/pricing-archived-correction-apply` updates open, unapproved [PR #363](https://github.com/sefaction/MTG-Archives/pull/363), stacked on #362/#359/#358/#357/#356. The previous local pilot rejected feeds with more than 500 changed identities. It now takes at most 500 differences from the verified feed, activates an immutable replacement generation, increments the queue's durable `applied_count` in the same transaction and leaves the feed `PENDING` until the last batch. A later run reconciles the same feed against the active archive and resumes. The isolated PostgreSQL fixture passed a 501-identity, two-run continuation (PENDING:500 then APPLIED:501, generation 2), alongside interruption, corrected price, added old identity, gap date, restore and replay checks. Typecheck and `git diff --check` passed. Push and CI are pending at this checkpoint. The running local Docker image still reflects the earlier cumulative #356-#363 plus #361 image `sha256:808efb6341d5da559377f0fc5b8a111956b41f2c8b9209a656009bae669c148c`; hold reload for the next coherent batches. No normal snapshot raw row was deleted.

The user chose the existing backup service as independent recovery target, but the repository documents only local `BACKUPS_DATA_PATH` and does not identify the service, remote path, or read-back verification. A focused question for those details is pending. No external copy or raw deletion has been enabled. Next safe step: push #363 revision, verify CI, then implement a disabled 2-5 a.m. America/Chicago maintenance scheduler with single owner, durable retries and backlog visibility. Keep #330 open and all unapproved PRs unmerged.

## Local archived correction apply pilot ready for review, 2026-09-25

Active branch `feat/pricing-archived-correction-apply` at the current worktree head stacks on green [PR #362](https://github.com/sefaction/MTG-Archives/pull/362). The new command reads the newest pending archived feed for one UTC date, verifies feed and active raw archive hashes, classifies corrections/additions/missing identities, and defaults to read-only. Its local-only apply restores a full Pricing backup, stages corrections in that isolated database, writes and restores a new immutable raw archive generation, simulates the archived basis plus visible summary transaction, then applies the same transaction on the live Pricing database after final state/hash checks. The transaction updates generation metadata, source/summary revisions and queue status together; old files remain. The isolated PostgreSQL fixture passed one changed old price, one new old identity, an absent prior date segment, missing-source preservation in the planner, interruption before activation, exact replacement restore, replay rejection and visible extrema parity. Typecheck passed; production build, CI and PR are pending. The command is capped at 500 effects and is not scheduled. Current running Docker remains cumulative #356–#359 image `sha256:29d11b0d27ad5b6ab0a808c6c5ebb0b0ba3dd20df3acb652b65e1216f399f262`. Independent #361 and dependent #362 are green, unapproved and not yet in that image. Next safe step: review diff, open PR, verify CI, then load a cumulative image with #361/#362/this batch before the next off-hours scheduler/backup/resource gate. No normal snapshot raw row was deleted and #330 remains open.

## Queued archived correction apply in progress, 2026-09-25

Active branch `feat/pricing-archived-correction-apply` stacks on green, open [PR #362](https://github.com/sefaction/MTG-Archives/pull/362), after #359/#358/#357/#356. It has a tested refactor that exposes the visible Pricing summary refresh body for inclusion in one larger correction transaction; existing isolated activation, sparse, correction and full-rebuild fixture still passes, as does typecheck. No processor code, PR or live Docker reload is complete yet. The next safe step is to implement the queue consumer: verify staged feed and old archive hashes, restore and reconcile one date in isolated staging, preserve missing-source identities, build and restore an immutable replacement generation, then atomically update archived daily basis, rebuilt archived/visible summaries, segment metadata, source/summary revision and queue receipt under locks. Prove corrected price, added old identity, replay and interruption in the isolated PostgreSQL fixture. Keep the 2–5 a.m. Central scheduler disabled until backup, resource and recovery gates pass. Running Docker still contains #356–#359 image `sha256:29d11b0d27ad5b6ab0a808c6c5ebb0b0ba3dd20df3acb652b65e1216f399f262`; #361 and #362 are green, independent/dependent respectively, unapproved and not loaded. No normal raw row was deleted. Do not merge any PR without individual approval.

## Archived Pricing daily correction basis in progress, 2026-09-25

Active branch `feat/pricing-archived-daily-basis` stacks on open [PR #359](https://github.com/sefaction/MTG-Archives/pull/359), after #358/#357/#356. It records one compact archived point per scope/day during guarded local raw activation, including representative price/provenance, raw count and latest ingest. A touched-scope archived rebuild reconstructs archived scope plus weekly/monthly/yearly extrema from that basis, refusing preexisting archive scopes without basis. The isolated PostgreSQL fixture passed parity against activated projections, one corrected old price, one newly added old identity, sparse scope, later live import and full rebuild. Typecheck passed; final build/CI/PR are pending. This branch is not in the running Docker review image, which still contains #356–#359 at `sha256:29d11b0d27ad5b6ab0a808c6c5ebb0b0ba3dd20df3acb652b65e1216f399f262`. Independent [PR #361](https://github.com/sefaction/MTG-Archives/pull/361) for #360 is green and unapproved. No queued old feed has been applied and no normal raw row deleted. Next safe step: open dependent PR, then implement verified immutable replacement generation and automatic bounded correction maintenance. Do not merge without individual approval.

## Cumulative Pricing raw archive review loaded, 2026-09-25

[PR #356](https://github.com/sefaction/MTG-Archives/pull/356) -> [#357](https://github.com/sefaction/MTG-Archives/pull/357) -> [#358](https://github.com/sefaction/MTG-Archives/pull/358) -> [#359](https://github.com/sefaction/MTG-Archives/pull/359) are open, green in both Core and PostgreSQL CI, and individually unapproved/unmerged. Active branch `feat/pricing-archived-feed-queue` at `fd7fd83` contains all four. The cumulative Docker image `sha256:29d11b0d27ad5b6ab0a808c6c5ebb0b0ba3dd20df3acb652b65e1216f399f262` is loaded into web, pricing-worker and notification-worker; web is healthy and host `/login` returned HTTP 200. The local read-only Pricing planner found 2,069,447 raw rows from June 30 to September 25, no row older than the June 27 90-day cutoff, fresh summaries and `rawDeletionEnabled: false`. The isolated PostgreSQL fixture passed sequential old-date activation, interrupted restore, feed fingerprint/spool replay and archive-aware refresh; typecheck, Compose configuration and production Docker build/client-manifest guards passed. No local snapshot raw row was deleted. #330 remains open: pending old corrections are queued but not yet applied, off-hours scheduling and external backup assurance are absent. Next safe step: build a bounded, retry-safe correction processor for queued old dates, then test the cumulative archive maintenance path before enabling any real retention schedule. #315 remains deferred and unapproved.

## Archived Pricing feed deferral in progress, 2026-09-25

Active branch `feat/pricing-archived-feed-queue` stacks on [PR #358](https://github.com/sefaction/MTG-Archives/pull/358), whose Core and PostgreSQL checks passed. This batch adds canonical per-date identity/price fingerprints to staged and activated raw segments; the importer partitions a full MTGJSON feed at `raw_archived_through`, spools only changed old dates into bounded gzip files and durable pending queue rows, and keeps normal upserts/refresh/current-price publication on newer dates. The upsert checks the expected boundary under a transaction lock. Missing source dates are logged as anomalies, never interpreted as mass deletion. Typecheck and the isolated PostgreSQL fixture pass, including fingerprint equality, deterministic spool replay, boundary guard, two old activations, interruption, and full rebuild. The running local services still use cumulative #356+#357 image `sha256:c919e449d9c051248cee166eb6c149af85e423b8739ba57c79fbb6cb19831f19`; this branch is not loaded yet under the user's batched Docker preference. No local snapshot raw rows were deleted, no queued correction processor or scheduler is active. Next safe step: final build/PR, then implement off-hours queued correction replay and replacement archives before a cumulative Docker reload. #356/#357/#358 await individual approval; keep #330 open.

## Guarded raw Pricing activation pilot in progress, 2026-09-25

Active branch `feat/pricing-raw-activation` stacks on open [PR #357](https://github.com/sefaction/MTG-Archives/pull/357), itself stacked on open [PR #356](https://github.com/sefaction/MTG-Archives/pull/356). This batch adds a local-only staged-segment activation command and durable segment metadata. It requires an oldest eligible raw date behind the daily compaction boundary; verifies the staged file, full database dump and isolated restore; simulates activation plus summary refresh before applying a guarded live transaction. The isolated PostgreSQL fixture passed two sequential old dates, sparse archived-only scope, interruption after restore, visible tier parity and full rebuild. Typecheck passed. One-off production Docker build passed before the last script/schema refinements; final branch CI and PR are pending. The running review services still use cumulative #356+#357 image `sha256:c919e449d9c051248cee166eb6c149af85e423b8739ba57c79fbb6cb19831f19`, not this new batch. No live raw rows were deleted and no scheduler or production operation is active. #330 remains open for host backup assurance, off-hours correction automation and resource/rollback gate. Next safe step: inspect the activation diff, open dependent PR, then continue another coherent batch before cumulative Docker reload. #356/#357 remain unapproved and unmerged.

## Verified raw archive staging review, 2026-09-25

Active branch `feat/pricing-raw-segment-archive` at `fa4d08e` is [PR #357](https://github.com/sefaction/MTG-Archives/pull/357), stacked on [PR #356](https://github.com/sefaction/MTG-Archives/pull/356) at `9cf8eb7`. Both Core and PostgreSQL checks passed on both PRs. The cumulative Docker image `sha256:c919e449d9c051248cee166eb6c149af85e423b8739ba57c79fbb6cb19831f19` is loaded into the local web, pricing and notification services; web is healthy and host `/login` returned HTTP 200. The isolated PostgreSQL fixture, existing correction/replay fixture, typecheck and production build passed. #356 supplies archive-aware tier refresh; #357 verifies immutable raw date-segment archives and restores. Neither activates archival, deletes raw data or schedules maintenance. #330 stays open. Both PRs await individual merge approval; #357 depends on #356. #315 remains deferred and unapproved.

Next safe step: continue #330 with a guarded archive activation/population batch, including backup and rollback checks, then implement automated off-hours older-correction handling. Reload local Docker after a few coherent batches. Reconcile live PR/CI and Docker status after interruption.

## Raw date-segment archive staging in progress, 2026-09-25

Active branch `feat/pricing-raw-segment-archive` is based on [PR #356](https://github.com/sefaction/MTG-Archives/pull/356), whose Core and PostgreSQL checks passed. This next #330 batch stages an eligible date's raw rows as a bounded, immutable gzip archive, reads the stored file back, restores into a disposable isolated database, compares exact serialized rows and price totals, rechecks the live source, and writes a `verified_staged` manifest with `activated: false`. Fresh summaries and the 90-day live boundary are required; no raw data is deleted or archive boundary advanced. The isolated fixture covers a populated old day, an empty day and a refused live day. Typecheck passed; final rebuild and fixture rerun are pending after the latest freshness guard. Local review services still run merged `main` image `sha256:7363ba6b00e654654687dc48571d1c04feddecffe75db60dd8d24973043d60f0`; this branch has only been built for one-off verification. Next safe step: rebuild, run isolated fixture, open dependent PR, then batch further non-acquisition work before reloading local Docker.

## Archive-aware Pricing refresh in review, 2026-09-25

Active branch `feat/pricing-archive-aware-refresh` from merged `main` (`448e4cd`) addresses the first implementation gate of [#330](https://github.com/sefaction/MTG-Archives/issues/330). It adds empty archived scope and weekly/monthly/yearly bases plus `raw_archived_through`; touched-key refresh merges archived and live projections, preserves sparse archived scopes, and rejects live raw overlap at an active boundary. Full rebuild enumerates archive-only keys. No archive activation, raw deletion or scheduler is enabled. The synthetic PostgreSQL test creates and drops an isolated database and passed split-period, sparse, overlap, later-import and full-rebuild cases. Existing correction/replay fixture passed against the local Pricing snapshot with the worker paused and resumed. Typecheck and production Docker build/manifest guards passed. The new image has been built but not loaded into the local review services; PR and CI are pending.

The uncommitted postmerge checkpoint from the previous turn is included in this branch. The currently loaded review containers still run merged `main` image `sha256:7363ba6b00e654654687dc48571d1c04feddecffe75db60dd8d24973043d60f0`. Next safe step: inspect the SQL diff and local DB state, open this coherent PR, then continue remaining non-acquisition queue work before a cumulative Docker reload. Keep #330 and #274 open; #315 remains deferred and unapproved.

## Approved raw archive planning merged, 2026-09-25

The user individually approved [PR #354](https://github.com/sefaction/MTG-Archives/pull/354) and [PR #355](https://github.com/sefaction/MTG-Archives/pull/355); both merged to `main` in that order at `448e4cd`. Documentation conflicts in #355 were resolved while preserving the design and planner instructions. Fresh Core and PostgreSQL checks passed on its final head. Local `main` matches `origin/main`. The merged Docker image `sha256:7363ba6b00e654654687dc48571d1c04feddecffe75db60dd8d24973043d60f0` is loaded into web, pricing-worker and notification-worker; web is healthy and host `/login` returned HTTP 200. The read-only planner reports 2,020,425 raw rows, zero older than its June 26 cutoff, fresh summaries and deletion disabled. #330 remains open for archive-aware refresh, verified raw retention and automated correction maintenance. #315 is the only open PR, is unapproved and remains unmerged. No scheduler, raw deletion or production deployment is active. This checkpoint update is local and uncommitted; next safe step is the next coherent #330 implementation batch.

## Raw archive planning premerge checkpoint, 2026-09-25

## Read-only raw retention planning, 2026-09-25

Active branch `feat/pricing-raw-retention-plan` from merged `main` (`fb8a1fe`) is [PR #355](https://github.com/sefaction/MTG-Archives/pull/355) for issue #330. It reports the 90-day default live-raw cutoff, candidate counts/dates, first 30 older observed days, raw table/index bytes and summary freshness in a repeatable read-only transaction; it cannot archive or delete. Typecheck, production Docker build/manifest guards and both PR CI checks passed. The local plan found 2,020,425 raw rows spanning June 30–September 24, zero rows older than the proposed June 26 cutoff, `summaryReady: true`, and `rawDeletionEnabled: false`; `--apply` was rejected. Docker image `sha256:aa6b4ade769d9f6c5f3abd421c4168f3c85b7e0ae59a6fad48a789d2241ecd3f` is loaded into web and both workers; web is healthy and host `/login` returned HTTP 200. The PR awaits individual approval.

The user explicitly approved #348, #349, #350, #351 and #353, and all merged into `main` after fresh Core and PostgreSQL checks. #335 and #352 are closed. Independent documentation [PR #354](https://github.com/sefaction/MTG-Archives/pull/354) describes the full off-hours archive/correction contract, is green, and awaits individual approval. #315 remains unapproved and unmerged. #330 and wider UI acceptance #274 remain open. No raw deletion, scheduler or production deployment is active. Next safe step: review #354 and #355 individually, then implement #330 archive-aware refresh and verified retention in separate batches. Keep the local review image loaded.

## Archive design premerge checkpoint, 2026-09-25

Base `main` at `fb8a1fe`, matching `origin/main`. Active branch `docs/pricing-raw-archive-design` at `9924a75` adds the #330 operating design in [PR #354](https://github.com/sefaction/MTG-Archives/pull/354), which is open for individual review. The user explicitly approved #348, #349, #350, #351 and #353; each merged in dependency order after fresh Core and PostgreSQL checks on updated heads. #315 remains open and has no merge approval. #335 and #352 are closed with resolution notes; #330 is open for the automated roughly 90-day live raw archive and off-hours correction workflow, and #274 remains open for broader UI acceptance. The user chose one digest per missed UTC import day, stale source status in Settings without an outage notice, about 90 days of live raw snapshots, and automated older-correction maintenance during 2–5 a.m. America/Chicago. No raw deletion or scheduled archive maintenance is active.

Merged `main` Docker image `sha256:d202774cb8610473c382a431ca07b329511d0de5fbec49b37c4b19402a97a6fe` is loaded into local web, pricing-worker and notification-worker. Web is healthy; host `/login` returned HTTP 200. The focused serial browser cases for the Pricing digest and three six-theme contrast paths passed 4/4. CI on every final merged head passed Core and PostgreSQL verification. #354 is documentation only, so this image also represents its application source. #354 CI is running. No production deployment. Next safe step: verify #354 CI, then implement #330's archive-aware refresh and retention gates in separate batches. Keep #315 unmerged.

## Pricing daily archive gate in progress, 2026-09-24

Active branch `feat/pricing-daily-archive-gate` is [PR #350](https://github.com/sefaction/MTG-Archives/pull/350), based on preservation [PR #349](https://github.com/sefaction/MTG-Archives/pull/349). The local-only compaction command defaults to dry run. Its apply path requires an explicit local opt-in, checks eligible daily/raw provenance and summary freshness, copies the full pricing database to a SHA-256 archive, restores it into a temporary isolated database for raw/daily/tier comparison, then checks unchanged live state before a guarded daily-row delete and boundary update. Raw snapshots remain intact. A synthetic January row passed the final full archive/restore/apply test with tier extrema comparison: one old daily row deleted, an applied receipt written, later raw correction updated monthly/yearly tiers, and no daily row reappeared. Fixture and cutoff were cleared; importer resumed; raw/daily counts returned to 1,971,376; temporary restore DB was removed. The 68,760,781-byte archive, manifest and receipt remain under `.local-data/backups/pricing`; its SHA-256 was confirmed separately. Typecheck and 596 unit tests pass. CI is pending; no merge or production deployment.

The review image `sha256:4c265feabdef73879186616b469c7b752462959571e3003039fcca28543e26ef` with #348 and #349 remains loaded and healthy; both PRs passed CI and focused browser cases. Next safe step: build cumulative Docker including this branch, verify the dry run and focused browser workflows, then open its dependent PR. Keep #330 open for raw-history archival/deletion and production rollback.

## Pricing daily preservation contract in progress, 2026-09-24

Active branch `feat/pricing-daily-compaction` from merged `main` (`26e51c0`) addresses a bounded next portion of [issue #330](https://github.com/sefaction/MTG-Archives/issues/330). Touched-key refresh now builds all long-range tiers from the complete raw-backed daily projection before honoring a recorded `daily_compacted_through` boundary. Standalone tier backfill refuses a compacted source; full raw-backed rebuild remains available. No compaction command or raw-history deletion is enabled. The old-date fixture in `scripts/verify-pricing-summaries.ts` passed against the local pricing database with a compacted daily row, later raw correction, and historical rollup parity; it restored the global boundary and cleaned its fixture. Typecheck passed. Docker build, cumulative review image, CI and PR are pending.

[PR #348](https://github.com/sefaction/MTG-Archives/pull/348) for #335 is open and both Core and PostgreSQL checks passed on its final head. Its code/image remain loaded in local Docker at this checkpoint. The Public #336-#338, Pricing #339-#343/#345/#347 and touch #346 stack merged into `main` at `26e51c0` on 2026-09-24; related #331-#334/#327-#329/#344 are closed. #330, #335, #274 and deferred planning PR #315 remain open. No new PR merge approval or production deployment. Next safe step: verify this branch's full tests and production build, open its PR, then build a cumulative local Docker image including both #348 and this preservation batch. Keep #330 open for archive manifest, restore, reviewed deletion and raw-history strategy.

## Pricing digest outage recovery in progress, 2026-09-24

Active branch `feat/pricing-digest-outage-recovery` from merged `main` (`26e51c0`) is [PR #348](https://github.com/sefaction/MTG-Archives/pull/348) for [issue #335](https://github.com/sefaction/MTG-Archives/issues/335). The user chose one in-app digest per missed UTC import day, plus stale source status in Settings without a separate outage notification. The branch adds a per-preference import-day cursor, a bounded oldest-first recovery scan (three import days per worker run, up to 90 days old), catch-up context on recovered digests, and a 72-hour Settings staleness display. The latest eligible day remains recheckable for delayed imports. It carries the prior uncommitted postmerge checkpoint below. Five focused tests, typecheck, 597 unit tests, production build/client-manifest guards, and the expanded four-owner Pricing digest browser case pass. Docker review image `sha256:c27380f46dd2aa547a4f4c4cdcc0ecffb710e23416b5aa0ab10dd60459108f2e` is loaded into healthy web and running pricing/notification workers; host `/login` returned HTTP 200. Browser fixture cleanup left 12,477 physical copies and no `ui-pricing-digest-*` users. Core and PostgreSQL CI were running when this checkpoint was written. No merge or production deployment.

Next safe step: verify #348 CI and continue with an independent #330 preservation batch. Reconcile actual Docker/build/PR state after any interruption.

## Approved Public, Pricing and touch stack merged, 2026-09-24

The user authorized merging completed PRs. Public #336 -> #337 -> #338, Pricing #339 -> #340 -> #341 -> #342 -> #343 -> #345 -> #347, and independent touch acceptance #346 merged in dependency order to `main` at `26e51c0`. Every retargeted branch passed fresh Core and PostgreSQL checks before merge. Conflict resolutions preserved both historical checkpoint sections in #343/#346 and all Pricing revision plus tier-state columns in #345. Local `main` fast-forwarded to the remote merge tip. Deferred Acquisition planning PR #315 is the only open PR; it remains unmerged and has a conflict.

Resolved Public #331-#334 and Pricing #327/#328/#329/#344 are closed with PR links. #330 was automatically closed by partial retention PR #342 and reopened with a note because raw/daily compaction and archive-preserving refresh remain unfinished. #335 stays open for source-outage and delayed-import notification behavior; #326 stays open as their parent. UI acceptance #274 remains open after #346's limited touch evidence. No production deployment.

Post-merge `main` Docker image `sha256:de2be61db0c24e3342086a2afee1ee3f951589ef416e07e1a3e04654eda33d24` is loaded into web, pricing-worker and notification-worker. Web is healthy and host `/login` returned HTTP 200. Focused serial browser cases for Pricing digest, Public parity and phone touch passed 3/3. Fixture cleanup left 12,477 physical copies and zero matching test users; Pricing state is ready/tier-ready with aligned source max ID and revision. The full browser suite was not rerun after merge; final branch CI passed. This checkpoint update is local and uncommitted, pending the next coherent documentation batch.

Next safe step: resume #335 source-outage/delayed-import review or #330 archive-preserving retention work. Reconcile live GitHub, git, Docker and processes first. Do not treat #315, #330, #335, or #274 as complete and do not infer any production release from these local merges.

## UI phone touch acceptance follow-up, 2026-09-23

Active branch `test/ui-touch-acceptance` from `main`. [PR #346](https://github.com/sefaction/MTG-Archives/pull/346) adds a touch-enabled Chromium phone path at 390 and 320 CSS px covering sign-in, navigation to Inventory, modal filter drafting/closing/reopening, and navigation to Settings. It asserts coarse pointer and no page overflow, and documents its limited evidence in `docs/UI_ACCEPTANCE_STATUS.md`. Both Core and PostgreSQL CI passed. The application source did not change; the existing cumulative Docker image `sha256:ad11e9ecb7bdcea6035b6ed47fc7c7ccfb74f8eb9421c581fa2ca9775bc118c4` remains loaded and healthy. Typecheck, 582 main-branch unit tests and three focused Inventory/navigation/touch browser cases passed. Fixture cleanup left zero touch users/players and 12,477 physical copies. Keep #274 open for real hardware/browser zoom, all-theme/role, and human task review. No merge or production deployment.
## Pricing alert retractions in review, 2026-09-24

Active branch `feat/pricing-digest-retractions` at `be124db`, stacked on Pricing correction PR #345 (`99f2d8d`). [PR #347](https://github.com/sefaction/MTG-Archives/pull/347) is open for issue #335; Core and PostgreSQL CI were running at this checkpoint. No PR was merged or issue closed. Corrections imported on the prior UTC day that put a previously **shown** owned movement below the owner's current threshold now create a follow-up entry in that day's in-app digest. It records the originally alerted and corrected prices, preserves the original digest, and deduplicates replay. A later alert can supersede an older retraction. Unshown movements beyond the 100-detail cap and no-longer-owned printings do not qualify. Email and webhook jobs remain absent.

The review image `sha256:77f2ee6c8b9322b48795fe6818856f7d8a978aa08c9b20d3e841bbb941a86052` was built from cumulative branch `local/cumulative-pricing-catchup-review` at `b852e6d`, including #336-#338, #342, and Pricing #339-#341/#343/#345/#347. Web, pricing-worker and notification-worker run this image locally; host `/login` returned HTTP 200. Typecheck, 595 unit tests, production build and client manifest guards passed. The focused four-owner browser lifecycle passed after a fixture time-of-day correction, covering a mixed digest, a retraction-only digest, source revision staleness, replay, ownership and zero outbound jobs. Fixture cleanup left 12,477 physical copies and zero test users/players. No production deployment.

Next safe step: verify PR #347 CI, then review #335 source-outage and delayed-import behavior. Keep #330 raw-history deletion blocked on the archive/refresh preservation contract. Leave all review PRs unmerged until individual approval. Reconcile GitHub, git, Docker and processes on resume.

## Pricing correction catch-up PR in review, 2026-09-23

Active branch `feat/pricing-digest-catchup` at `f9c2a75`, based on opt-in digest PR #343. Source review found that the MTGJSON importer ignored a changed price for an already imported exact printing/day; this is [issue #344](https://github.com/sefaction/MTG-Archives/issues/344). The branch now upserts changed prices, records revision and corrected-job counts, leaves same-value replay untouched, and refreshes the daily/long-range projections. Pricing views and daily digest generation wait while revision state is stale. The digest selects newly imported or corrected observations from the prior UTC import day, including observed dates up to 90 days old, with a correction marker. This completes the same-day correction bug; broader alert semantics in #335 remain open for review, including changes that drop below the threshold, source outages and delivery behavior.

Combined local Docker branch `local/cumulative-pricing-catchup-review` at `615cf41` includes this work, Pricing retention PR #342, and Public PRs #336-#338. Image `sha256:ad11e9ecb7bdcea6035b6ed47fc7c7ccfb74f8eb9421c581fa2ca9775bc118c4` is loaded in web, pricing-worker and notification-worker with local/SMTP overlays; web is healthy. Production build and client-manifest checks, typecheck, 595 cumulative unit tests, and three focused browser cases passed. The digest fixture covered 105 meaningful movers capped at 100, four owners with uneven settings, same-price replay, an older corrected observation, next-day catch-up, no delivery jobs, stale-summary guard, and owner isolation. Fixture cleanup left zero test users, 12,477 physical copies, and ready/tier-ready Pricing summaries with matching max ID and revision.

[PR #345](https://github.com/sefaction/MTG-Archives/pull/345) is open against #343 and both Core and PostgreSQL CI passed. Issue #344 is linked and remains open until merge. No PR has individual merge approval; no production deployment. The #330 raw/daily compaction gate remains deferred because current touched-key refresh rebuilds tiers from raw/daily and would erase older rollups after pruning; the local snapshot is less than 90 days old, so immediate pruning would save no rows. A new local branch `feat/pricing-retention-preservation` was created from #342 to inspect a safe refresh contract, with no changes yet. Next safe step: design and test non-deleting preservation behavior or move to another independent issue if deletion needs a product decision. Keep #330 and #335 open.

## Daily Pricing digest PR opened, 2026-09-23

[PR #343](https://github.com/sefaction/MTG-Archives/pull/343) is open from `feat/pricing-alerts` against #341. Both CI jobs were pending when this checkpoint was written; verify them before calling the PR green. The cumulative local Docker image, migration, 595 units, typecheck and focused browser 3/3 passed as recorded below. Public #336 → #337 → #338 and Pricing #339 → #340 → #341 are the dependency chains; #342 and #343 both follow #341 independently. No PR has individual merge approval. Issue #335 stays open for delayed-correction catch-up; #330 stays open for the safe pruning gate. Next safe step: inspect #343 CI and report the review order. No merge or production deployment.

## Pricing digest ready for PR, 2026-09-23

Active branch `feat/pricing-alerts` at `05945d0`, based on #341 (`feat/pricing-owned-movers`). User selected one opt-in daily in-app digest with a default $2 per-card change. The branch adds owner preferences/settings, daily notification-worker generation, owner-scoped digest detail, an additive migration, unit/browser coverage and [PRICING_DIGESTS.md](PRICING_DIGESTS.md). Digests are bounded to 100 detail rows, deduplicated by owner/day and never enqueued for email or webhook. A 30-minute retry avoids repeated quiet-day collection scans while admitting delayed imports. Immutable digest snapshots link to live corrected history; a correction imported after the next-day window is not separately alerted and remains a follow-up limitation for #335.

Local review image `sha256:481303113b3f43972e8642320a3b5e33a6cb214b16ce0907ebf129311334be07` combines this branch with #342 and Public #336-#338 on `local/cumulative-pricing-digest-review`. It is loaded into web, pricing-worker and notification-worker with local/SMTP overlays. The additive preference migration applied; web is healthy and pricing summary state is ready, tier-ready and aligned with raw max ID. Production build and client-manifest guards, typecheck, 595 cumulative unit tests, new four-owner/113-printing digest browser case (1/1), and owned movers/Public regression cases (2/2) passed. The digest test verified 105 meaningful movers capped at 100 details, three opted-in owners and one opted-out, no delivery jobs, worker replay, and isolated signed-in views. It cleaned all fixture users/history; physical inventory remains 12,477 copies.

Review stack is Public #336 → #337 → #338, Pricing #339 → #340 → #341 → #342. #341/#342 CI passes. Acquisition planning #315 remains open. None have individual merge approval. #330 still needs a safe raw/daily pruning gate; #335 should remain open for late-correction catch-up review. Next safe step: push this branch, open a review-ready PR against #341 and verify CI; then review remaining issue priorities. Do not merge, close #335, or deploy.
## Pricing time tiers and restore gate, 2026-09-23

Active branch `feat/pricing-retention-verification` at `b2ab661`, stacked on owned-movers PR #341. User prefers logical time tiers; current defaults are 90 days daily, two years weekly, ten years monthly, then yearly points, with validated environment overrides. Each rollup records open/close/min/max prices and dates. A new one-time tier backfill and readiness state let long-term card history fall back to monthly until weekly/yearly rows are complete. This is a first review batch toward #330; **no raw or daily rows were pruned**, and issue #330 stays open for the later archival/compaction gate. See [PRICING_RETENTION.md](PRICING_RETENTION.md).

Separate-pricing-database backup was validated locally: 60,724,806-byte custom-format dump copied under `.local-data/backups/pricing/`; the copied archive restored into an isolated database with matching 1,971,376 raw and daily rows and 153,875 monthly rows, date ranges, and price sums. The disposable restore database was dropped. Tier backfill took 177,842 ms and produced 650,052 weekly rows (269 MB) and 52,326 yearly rows (21 MB); 100 weekly and 100 yearly sampled keys matched daily observations with zero mismatches. One `/login` request during backfill completed in about 183 ms. Current local raw history spans roughly 85 days, so a 90-day cutoff would remove nothing today.

Cumulative local Docker image `sha256:78653c6e412eb2a2f49a1ddde7964a57fcc94773d904a4cc93fa6849b2d57329` combines Pricing through `b2ab661` with Public PRs #336-#338. Web and pricing-worker are loaded with the local and SMTP overlays. Production build/client-manifest guards, 592 unit tests, typecheck, and focused Pricing/Public browser checks 2/2 passed. The Pricing browser fixture covered all four historical resolutions and cleaned its user; summary state is ready, tier-ready and aligned with raw max ID. Next safe step: push this branch, open a PR based on #341, and verify CI. No PR merges or issue closures have occurred.

## Owned Pricing movers review, 2026-09-23

Active branch `feat/pricing-owned-movers` at `9a9a8d0`, stacked on Pricing summaries PR #340. User selected a $2 change per card as the default meaningful movement; percent and prior-price filters are optional. The branch implements exact owned-printing daily movers with finish-aware quantities, weighted collection impact, source/currency filters, stale and missing coverage, and a bounded 7/30/90-day or monthly card-history drilldown for an owned card. No notification delivery is included; #335 remains the later opt-in notification issue. Issue #329 stays open until its PR merges.

The cumulative local Docker image `sha256:24aae6e478202556d6a46ba03822f703b5740fbaef9a79987e992f5bce86ee39` combines Pricing through the #329 application commit `c193a59` and the Public review stack through #338. It is loaded in web and pricing-worker using `docker-compose.yml`, `docker-compose.local.yml`, and `docker-compose.smtp-test.yml`; local databases retain their mounted data. The final #329 commit `9a9a8d0` changes only the browser test. Production Next build and manifest guards passed. 591 unit tests, typecheck, and focused browser tests 2/2 (owned movers and Public parity) passed. The browser fixture covered a rare gain, sub-$2 noise, loss, stale source, absent prior, zero prior, duplicate stacks, sold card, foil, EUR, corrected daily observation, weighted impact, card drilldown, and 390px layout. Fixture cleanup left zero `ui-pricing-movers-*` users; Pricing summary state is ready and aligned with raw max ID. The previous local image/branch details in the section below are historical.

Next safe step: push this branch and open a review-ready PR for #329 against #340; verify both CI jobs. Then begin #330 verified retention/backfill, retaining #315 planning and all other open PRs for review. Do not merge or close #329 before approval/merge.

## Active queue, Public review stack and Pricing diagnosis, 2026-09-23

Pricing review PRs are open in order: [#339](https://github.com/sefaction/MTG-Archives/pull/339) #327 request isolation and [#340](https://github.com/sefaction/MTG-Archives/pull/340) #328 keyed summaries based on #339. Core and PostgreSQL CI passed on both. PR #339 alone keeps unrelated routes responsive but may show unavailable Pricing after a 10-second raw-query bound for the largest owner; review it together with #340 before treating the user-facing Pricing problem as fixed. Current branch `feat/pricing-summaries` at `5eb0e2c`; no uncommitted source. The separate local branch `local/cumulative-pricing-public-review` merges Pricing and Public PR code for the Docker build (branch commit `61fa2db`, before a later worker index-recovery hardening change that does not affect web runtime).

The local pricing database was backfilled from 1,971,376 raw rows: 5,734 printings in 217,147 ms. An isolated fixture passed repeated rebuild and corrected-observation checks; 100 sampled scopes and 100 monthly summaries matched raw history with zero mismatches. Summary sizes were about 22 MB scope, 747 MB daily including indexes and 63 MB monthly; all raw observations remain. The daily size demonstrates why #330's verified retention/compaction gate still matters. The cumulative Docker image `sha256:925a17247e0df546d268168ac2db4a519c1a627e8f95da12143f7f99e34623ba` is loaded and healthy with Public and Pricing source. Production build and client-manifest guards passed. A disposable 4,146-printing owner measured five `/pricing` requests per view: Collection p50/p95 3,426/11,011 ms (first/cold outlier), Market 787/2,592 ms, Data 1,893/2,407 ms; concurrent Dashboard 224 ms. The scale fixture was removed, zero `ui-pricing-scale-*` users remain, and inventory returned to 12,477 physical copies. Four existing Pricing/Inventory/League browser checks and the Public parity fixture passed on this cumulative image. 590 unit tests and typecheck passed before the final test-only/docs additions; rerun affected checks as needed. No merges or issue closures.

User activated all 22 previously held issues. Live GitHub shows no remaining `on hold` labels; new Pricing notification issue #335 is active after the user chose an in-app movers view first with notifications planned. Hardware for physical fi-6130Z issue #313 is not yet available, so virtual/source-independent work precedes that physical gate. Priority/dependency order is in [ISSUE_PRIORITY.md](ISSUE_PRIORITY.md). Planning PR #315 remains open and unmerged; no PR merges or production deployment were authorized.

Active branch `fix/pricing-responsive-queries` at `9422735` from `main` `a714f4f`, working on #327. Pricing history reads are bounded and asynchronous, active views are isolated, Market uses a lighter owned-card projection, empty holdings skip history, and loading/retry states are present. A UUID-led pricing-history index is added to worker schema initialization. The local pricing snapshot has 1,971,376 rows; one printing's stats lookup fell from 2,052.7 ms/5,457 buffer pages to 1.3 ms/5 pages, while a 100-printing sample still chose a full scan at 4,723 ms; a forced indexed lateral plan ran in 967 ms. The new index occupies 178 MB.

The first realistic dashboard-function benchmark for the largest current owner (4,146 distinct MTGJSON printings) returned unavailable after about 10.1 seconds for each of Collection, Market and Data. This confirms that async timeouts protect unrelated requests but do not make Pricing useful at current collection size. Do not present #327 as a complete performance fix. Next, profile/replace the request-time history aggregation under #328, then rerun named p50/p95 and concurrent navigation checks before publishing Pricing review PRs.

Independent Public review PRs are open in order: [#336](https://github.com/sefaction/MTG-Archives/pull/336) audit #332, [#337](https://github.com/sefaction/MTG-Archives/pull/337) workspace #333 based on #336, and [#338](https://github.com/sefaction/MTG-Archives/pull/338) parity gate #334 based on #337. Core and PostgreSQL CI passed on all three. The cumulative local Docker web and pricing-worker image `sha256:b7f10b1e940eac62b16c025d4fe03f0597d86e6d1181b695fe6979b37c4ff751` is healthy and contains both the Public and preliminary Pricing source, although the Public branches are now independent of Pricing. Local verification: 582 unit tests, typecheck, production Docker build/manifest guards, Public browser 1/1 at 1366/390/320 and six themes, existing Inventory/Public/League 3/3, and Pricing nonblocking browser 1/1. No merges or issue closures.

Next safe step: check Public PR CI; build bounded summaries for Pricing #328, remeasure against realistic owned-card scope and inspect browser output. Keep every issue open until its resolving PR merges.

## Two new initiatives planned and deferred, 2026-09-23

Current branch `main` at `a714f4f`; application code and Docker were not changed for this planning request. The previously recorded post-merge checkpoint edit remains local and uncommitted. The user requested issue plans only and explicitly deferred implementation.

Pricing roadmap [#326](https://github.com/sefaction/MTG-Archives/issues/326) has four on-hold issues: #327 request-path diagnosis and responsiveness, #328 daily/long-range summaries, #329 owned-printing movers, and #330 verified backfill/retention. Local read-only measurement: 1,971,376 `price_snapshots` rows, table plus indexes about 753 MB. Source review shows the Pricing route loads all owned stacks and runs broad history analytics for every view through synchronous `psql`; these are plausible contributors, not a completed request profile. Foundry plan: `MTG Archives - Pricing Sustainability Plan.md`.

Cross-surface roadmap [#331](https://github.com/sefaction/MTG-Archives/issues/331) has three on-hold issues: #332 private/Public/League role-task audit, #333 public workspace parity, and #334 ongoing regression guardrails. Public already reuses shared search/filter/browser components but lacks the private `InventoryWorkspace` composition and retains the older filter disclosure. Preserve public read-only scope and League season rules. Foundry plan: `MTG Archives - Public and League Inventory Parity Plan.md`.

No implementation, PR, merge, or production deployment was performed for these initiatives. Existing open UI acceptance issues and Acquisition planning PR #315 are unchanged. Next safe step: user prioritizes or authorizes a specific child issue; begin with #327 for Pricing and #332 for cross-surface parity.

## Approved review stack merged, 2026-09-23

User approved the completed work and excluded planning PR #315. PRs [#316](https://github.com/sefaction/MTG-Archives/pull/316), [#320](https://github.com/sefaction/MTG-Archives/pull/320), [#322](https://github.com/sefaction/MTG-Archives/pull/322), [#324](https://github.com/sefaction/MTG-Archives/pull/324), and [#325](https://github.com/sefaction/MTG-Archives/pull/325) merged into `main` in dependency order, ending at `a714f4f`. #325 was updated against current main and both required CI checks passed before merge. The local `main` branch was fast-forwarded to this commit. No production deployment.

Issues #301, #271, #317, #318, #272, #319, #273, #321, and #323 are closed with resolving PR references. Six UI foundation/acceptance issues #262/#263/#264/#265/#268/#274 remain open for final user workflow review and the documented manual contrast, zoom, touch, keyboard, and task-observation gates. Thirteen Acquisition issues #302-#314 remain on hold; documentation-only PR #315 stays open. No further Playtest or Acquisition implementation is authorized by this merge.

The cumulative local Docker web image remains healthy at `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70`, and `/login` returned HTTP 200 after the merge. The image includes all application changes through #324; #325 adds only tests and documentation. Its prior 51/51 serial browser run and all PR CI checks are recorded below. Next safe step: decide whether to complete the six open UI acceptance issues or leave them tracked while selecting a new priority; do not infer they are done solely from this merge. This checkpoint update is local and uncommitted, pending the next authorized documentation batch.

## Acceptance PR in review, 2026-09-23

PR [#325](https://github.com/sefaction/MTG-Archives/pull/325) is open against #324. Initial Core and PostgreSQL CI passed (run 35932802802). Final serial browser 51/51 passed in 7.8 minutes on the healthy cumulative image; the test correction to Admin Users changed no app source. Route/action crosswalk: 34 page routes, 45 IDs. Fixture cleanup: zero `ui-*` users, 12,477 physical copies. Review order #316 -> #320 -> #322 -> #324 -> #325; independent acquisition planning #315 remains separate. No merge, issue closure or production deployment. Remaining manual user gates: final task usability, actual browser zoom/touch, comprehensive contrast/keyboard and role combinations. After this documentation checkpoint commit, recheck CI on the new PR head before calling it green.


## Acceptance validation complete locally, 2026-09-23

Branch `docs/ui-acceptance-reconciliation` on cumulative Administration PR #324; no application source changes. The updated auth-session regression now opens Admin Users after #324 navigation; focused security lifecycle passed (34s). Final full serial browser suite passed **51/51 in 7.8 minutes** on image `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70`. Zero `ui-*` fixture users, 12,477 physical copies, healthy web. Crosswalk has 34 live page routes and 45 action IDs. Acceptance document records exact behavioral evidence and unresolved manual/user review gates. Next safe step: typecheck/document checks, commit/push, open a review-ready acceptance PR against #324, resolve CI and update Foundry durable milestone. No merges, issue closures or production deployment.


## New acceptance goal, 2026-09-23

Live GitHub: 28 open issues, 13 on-hold Acquisition issues, 9 active issues addressed by open PRs #316/#320/#322/#324, and 6 remaining UI foundation/acceptance issues #262/#263/#264/#265/#268/#274. Five PRs open including independent acquisition planning #315; all CI green. This goal focuses on cumulative acceptance, final regression and a review-ready documentation PR; no merges, production, acquisition implementation or further Playtest changes. Current clean branch `docs/ui-acceptance-reconciliation` at `78efea0`, based on #324. Running Docker web image `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70` healthy, `/login` HTTP 200. Next: full serial browser regression, then inspect results and fixtures.



## Acceptance regression follow-up, 2026-09-23

First complete serial 51-case run on the cumulative Docker image finished **50 passed / 1 failed** after 8.4 minutes. The failure was `tests/ui/auth-sessions.spec.ts` navigating `/admin` for password reset, while #324 moved that unchanged action to `/admin?view=users`. It timed out before performing the reset; no security behavior failed. The test now navigates to Users and asserts the active task; the focused reset/session invalidation case passed in 34 seconds. No app source or Docker rebuild changed. A clean full 51-case rerun is currently active (shell session 24890, log `.git/acceptance-full-browser-final.log`); inspect its terminal result before claiming the gate passed. Prior failed-run fixtures were zero and the database held 12,477 physical copies before rerun.

## Goal closed at user request

User requested a final review rundown and closure of this autonomous goal. Live reconciliation confirmed five open PRs: implementation review order #316 -> #320 -> #322 -> #324, plus independent acquisition planning #315. All reported CI checks pass. The cumulative Administration image remains running and healthy. No merges or production deployment were authorized or performed.

The stopped full-suite run (29 passing cases before interruption), remaining manual acceptance, and unpublished acceptance-documentation PR are explicitly deferred to a future task, not declared finished. Acceptance draft branch `docs/ui-acceptance-reconciliation` is pushed; see UI_ACCEPTANCE_STATUS.md. The pause/recovery details below remain the technical starting point if that separate work is requested.


## User-requested pause, 2026-09-23 11:42 UTC

User is shutting down laptop and explicitly requested pause. Active branch `docs/ui-acceptance-reconciliation`, based on Administration `9493d6c`. Four new review PRs are open in order #316 -> #320 -> #322 -> #324; all Core and PostgreSQL CI green. #324 run 35855847657 includes new pricing PostgreSQL checks. #315 remains independent/deferred planning. No merges/closures/production deployments.

Current healthy local image `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70` includes all four application batches through `8776e77`; later changes are tests/docs. Full serial regression was explicitly interrupted at user request after 29 passing cases, during the existing Playtest regression (no Playtest implementation changes). It is NOT a completed full-suite pass. `.git/combined-browser.log` preserves partial output; execution session 73522 is terminal and no Playwright Node processes remain. The interrupted UUID-owned Playtest fixture was cleaned using its existing user-cascade cleanup; verified zero ui-* fixture users and 12,477 physical copies.

Acceptance documentation draft lives in docs/UI_ACCEPTANCE_STATUS.md with historical crosswalk/feature-note links updated. All 34 page routes are represented. Fresh scale case passed with 1,200 nodes, 1,000 printings, 150,000 copies and four uneven owners (479ms local page load, 81,727 HTML bytes, 12 options). No blanket accessibility/acceptance certification.

Next safe step: reconcile git/GitHub/Docker after restart; bring up existing local Compose without unintentionally rebuilding or deploying; rerun the interrupted full serial browser suite on the recorded image. Update acceptance evidence and morning handoff, publish the docs review PR against #324, then report review order and remaining manual gates. No pending consequential product questions. Acquisition/on-hold and further Playtest changes remain deferred.


## Resumed by user, 2026-09-23

User explicitly requested continuation after the original window. Active branch `feat/admin-task-workspaces`, checkpoint commit `8776e77`. Typecheck including new browser test passed. Administration image `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70` is now loaded and healthy, cumulative with #316/#320/#322. Real PostgreSQL pricing query checks passed (Unicode/zero/empty, nonblocking query, bounded timeout, sanitized failure, read-only). Focused existing Administration browser checks passed 3/3 and the new comprehensive workflow passed (55.4 seconds) after correcting test button/30-job expectations and making cleanup unconditional. Pricing health loaded in 632ms; a concurrent dashboard completed in 115ms during exact totals calculation (1,922,270 snapshots). All task routes passed desktop/390/320px, six themes/enlarged text and admin/member/anonymous boundaries. Visually inspected phone pricing and desktop user screenshots. Cleanup: zero Admin fixture users, 12,477 physical copies. Next: publish the Administration PR against #322 and verify CI; then run combined regression/acceptance reconciliation. No merges or production deployment authorized. Earlier stop entry below is historical.

## Morning stop, 2026-09-23 11:25 UTC

The authorized eight-hour window has elapsed. Implementation is stopped; resume only with a new instruction. Three new review-ready PRs are open, in dependency order: #316, #320, #322. All have green Core verification and PostgreSQL import integrity CI. No PRs merged, issues closed or production deployed. Existing acquisition documentation PR #315 remains separate.

Active branch: `feat/admin-task-workspaces`, based on #322 commit `730590c`. Administration changes for #273/#321/#323 are work in progress, not review-ready. Shared Admin navigation, Users task/search, bounded job/backup lists, maintenance disclosures, asynchronous pricing health and on-demand exact totals are implemented. Pricing auto-refresh now preserves client state. 582 unit tests, typecheck and Docker production build with ten manifest guards passed before the new browser test was added. The new real PostgreSQL pricing script and Administration browser test have NOT run; do not claim those checks passed. The newly added browser test also needs typecheck.

Built Administration image: `sha256:09053d1ce741b62d1372c7cc0bad41f52c6302d84fbc9e4c44cb9cd04b86ff70` (tag mtg-archives-web:local), NOT loaded. Running healthy web remains `sha256:d870a16d3c1a515b77d0fce47245b6abd343d3d59eefd837cd10fea7f65a8c92`, containing app commit `5ab335b`, cumulative #316 + #320 + #322. Local review: http://127.0.0.1:13001. Worker images unchanged.

Next safe step after renewed authorization: reconcile git/processes/containers; typecheck the Administration test; load the built image only after confirming its source; run scripts/verify-pricing-db-query.ts against local PostgreSQL and tests/ui/admin-workspaces.spec.ts plus affected existing admin tests. Fix failures, visually inspect desktop/phone screenshots, verify fixture cleanup, then commit/push and open the Administration PR against #322. Retest Core/PG CI. Do not merge.

Remaining eligible work: #273/#321/#323 validation/publication; acceptance reconciliation #263/#264/#265/#268 (non-playtest), #274/#262. No consequential unanswered product question is recorded. Acquisition #302-314/on-hold and further Playtest remain deferred. See docs/MORNING_HANDOFF.md.

## Autonomous review batches in progress, 2026-09-23

- Authorized window: 2026-09-23 03:21:29 UTC through **11:21:29 UTC at the latest** (eight hours). No merges, production deployment, acquisition implementation or further playtest changes. All on-hold issues stay deferred. Close resolved issues only after future approved merges.
- Review PR #316 (`fix/import-commit-integrity`, app `8a3279c`, docs `751cdc4`) is open on main `bfb7be8`. Core and PostgreSQL CI passed (run 35815121834). It fixes #301 only on future merge. Acquisition documentation PR #315 stays independent/unmerged; no acquisition or playtest changes.
- Review PR #320 (`feat/browse-pricing-workspaces`, app `5559c2c`, docs `93fa844`) is open against #316. Both CI jobs passed (35815990855). It addresses #271/#317/#318 only on future merge. 582 units, typecheck/build/guards, new browser coverage and four affected Deck/Inventory cases passed.
- PR #322 (`feat/league-task-workspaces`, app `5ab335b`, docs `730590c`) is open against #320; both CI jobs passed (35818495858). #272 live audit completed on a populated lifecycle before coding; reproduced 390px season management overflow and filed #319. New season task views, searchable choice controls, explicit frozen deck status, filtered/bounded library/history, scoped statistics views and task-aware validation redirects are implemented. Typecheck and 582 units pass. First expanded browser run completed game/freeze/conservation checks but timed out on the new history selector label; fixed its explicit accessible name without increasing waits. Corrected accidental new text separators, added two League manifest guards; final Docker build and nine manifest guards passed; expanded League browser lifecycle passed (24 seconds) including every task at desktop/390/320, themes/enlarged text and all original data/scope invariants.
- Current healthy Docker image `sha256:d870a16d3c1a515b77d0fce47245b6abd343d3d59eefd837cd10fea7f65a8c92` includes #316 + #320 + final League edits. Worker images unchanged. No recurring scheduler/restart guarantee claimed.
- Active branch `feat/admin-task-workspaces` for #273/#321/#323, stacked on #322. Administration navigation, task disclosure, bounded user/job/backup lists and asynchronous on-demand pricing totals are in progress. Initial typecheck passed before the additional task splits; current edits are not yet built or browser-verified. Live baseline audit found notification history reaching 10,154px on phone and pricing phone overflow (#323). The pricing-health aggregate took 31,686.552ms over 1,922,270 rows (#321).
- Next safe step: finish Administration behavior/validation, build cumulatively, run browser and real-PostgreSQL checks, then publish the Administration PR against #322. Do not assume uncommitted edits are loaded in Docker.
- Remaining acceptance queue: reconcile #263/#264/#265/#268 (non-playtest) and #274/#262. No consequential unanswered question currently blocks the queue. No issues closed and no PRs merged.



## Account, wishlist/trade, and vault fixes merged, 2026-09-23

- User-approved PRs #297, #298, and #299 are merged into `main` through `43767fa`. Issues #260, #269, and #270 are closed with resolving PR links.
- The merged batches cover fresh vault occupancy after moves, clearer Wishlist/Trade actions, and Settings-only global navigation with account options inside Settings. No production deployment occurred.
- Next safe step: define and record the next roadmap project before implementation. Main is clean and synchronized with origin.

## Account options moved under Settings, 2026-09-23

- PR #299 for #270 is revised on branch `feat/account-settings-navigation` at `dd7c0f9`. The global navigation now exposes only Settings; the Settings workspace contains Password, Email delivery and Webhooks under `Account and settings`. Existing routes, save scopes, delivery semantics and session behavior are unchanged.
- Verification passed: typecheck, 579 unit tests, production Docker build and client-manifest guards, focused navigation/notification/email browser coverage **3/3**, healthy compose web and `/login` HTTP 200. Local image is `sha256:d61f798630ff8256651b55c43e93a923c4159547d2a4a6d7ee1b44efff3c8ba9`.
- Next safe step: keep PR #299 open for review; do not merge without separate user approval. PRs #297 and #298 remain open and unmerged.

## Account and settings navigation ready for review, 2026-09-23

- Branch `feat/account-settings-navigation` is at `569324e` for #270. The shared navigation now groups Settings, Password, Email delivery and Webhooks under â€œAccount & settingsâ€; existing routes, save scopes, delivery semantics and session behavior are unchanged.
- Verification passed: typecheck, 579 unit tests, production Docker build and client-manifest guards, focused navigation/notification/email browser coverage **3/3**, healthy compose web and `/login` HTTP 200. Local image is `sha256:f3470c343c3e47479670998a2b61bbad993b197726a92ede58750f7e89730fb0`.
- Next safe step: push this branch and open a review PR for #270. Do not merge without separate user approval. PRs #297 and #298 remain open and unmerged.

## Wishlist and Trade workspace ready for review, 2026-09-23

- Branch `feat/wishlist-trade-workspace` is at `a6c6917` for #269. Wishlist results and filters now precede manual capture; quantity guidance and manual add are explicit collapsed tasks. Empty Trade Desk lanes link directly to the existing proposal builder and inventory searches. Wishlist/trade semantics, reservations, visibility and lifecycle actions are unchanged.
- Verification passed: typecheck, 579 unit tests, production build and client-manifest guards, full 49/49 serial browser cases, and focused `tests/ui/trade-wishlist.spec.ts --repeat-each=3` (**6/6**). Docker image is `sha256:37dd7a12b711a60b1e3ffa2b7670f35560cc2468b4ef41f45e153b8d3680c01b`; compose web is healthy and `/login` returned HTTP 200.
- Next safe step: push the branch and open one review PR for #269. Do not merge without separate user approval. PR #297 for #260 remains open and unmerged by user direction.

## Vault occupancy PR open; filter audit closed, 2026-09-22

- PR [#297](https://github.com/sefaction/MTG-Archives/pull/297) remains open and unmerged by user direction while additional reliability work proceeds. Its branch is `fix/vault-occupancy-refresh`; local image and 5/5 vault-map evidence are recorded immediately below.
- Issue [#220](https://github.com/sefaction/MTG-Archives/issues/220) is now closed after the existing document-navigation fix from [PR #221](https://github.com/sefaction/MTG-Archives/pull/221) passed the unchanged `tests/ui/inventory-color-filter.spec.ts --repeat-each=20` audit: **40/40** cases passed on the current Docker stack.
- Next safe step: independently audit #280 (deck-analysis remote-image timeout) without changing timeouts or weakening rendered/private-access assertions. Keep #260 and #280 separate.

## Vault occupancy refresh ready for review, 2026-09-22

- Branch `fix/vault-occupancy-refresh` is at `fa6ef7f` for #260. The committed move action now returns fresh source/destination occupancy summaries, and the inventory client applies them immediately before the normal route refresh. No reload, timeout, schema, or migration workaround was added.
- Verification passed: 580 source tests, typecheck, production Docker build and client-manifest guards, healthy compose web service with `/login` HTTP 200, and `tests/ui/vault-map.spec.ts --repeat-each=5` (**5/5**). Local image is `sha256:7f7f03e02e00cff8e0e6cdc2691cc0ed18586f4a91e96fd138be3a8361c3bf27`.
- Next safe step: push this branch and open the review PR for #260. Do not merge until the user separately approves that PR; #260 remains open until then. #220 and #280 remain separate audits.

## Playtest organization released, 2026-09-22

- PR #295 was user-approved and merged into main as `fb9e2cd`; the existing playtest PR #296 was then approved and merged as `e25fd23`. No further playtest changes are planned in this batch.
- The released #296 work labels frequent actions and separates Battlefield workspace and Selected cards from token/library/random and player/commander tools. Sandbox state, device-local storage, limits and authoritative deck boundaries are unchanged.
- Verification passed 579 tests, typecheck/build/manifest guards, Docker health and 3/3 focused playtest browser cases. Local image evidence remains `sha256:6433b3a2c727639a79f237c3c12691ccd4bfaea704ba89096852417fd714985c`.
- Next safe step: begin the reliability queue with #260 (stale vault occupancy after successful move), preserving unchanged UI assertions and not claiming a fix until the root cause is demonstrated. #220 and #280 remain separate follow-up audits.

## Deck library workspace paused, 2026-09-22

- Branch `feat/deck-library-workspace` is clean at commit `535b87a` (`Record deck library browser verification`), pushed to origin, with PR [#295](https://github.com/sefaction/MTG-Archives/pull/295) open and awaiting review. It is not merge-approved.
- This batch adds a visible Deck library control region, result summary, primary `+ New deck` entry point, accessible Table/Cards pressed state, controlled create disclosure, and actionable empty results with Clear filters / New deck recovery. Existing search, folder, tag, bracket, ownership, read-only, and server-action behavior is preserved.
- Verification completed: focused deck source test plus full `npm run verify:core` passed with **578 tests**, typecheck, production build, and client manifest guards. Docker image `sha256:e38e7bb62f1baeea746074f78a44c412363aa5483b4a5c180687761084ca2868` is loaded locally; compose web service is healthy and `/login` returned HTTP 200. Focused Decks browser run passed **7/7** (`decks-brackets`, analysis, sample hands, playtest) after renaming the new top action to `Create deck` to preserve the existing `+ New deck` locator.
- Next safe step: user review of PR #295; merge only after the user's separate approval. After merge, continue #268 with deeper playtest/grouping work.

## Deck builder workspace ready for review, 2026-09-22

- User-approved #288 and #291 merged in dependency order, ending at main `5a8b77ce59af9ba34028dfd46f8e9046edbc3379`. Resolved #283/#289/#267/#290/#292/#293 are closed with notes. Refreshed #291 CI 35750031622 and main publication 35750201468 passed. No production deployment.
- Branch `feat/deck-builder-workspace`, PR #294 (https://github.com/sefaction/MTG-Archives/pull/294), application tip `82f8fac`. First coherent portion of #268: compact identity, expandable art/coverage, direct Add/Paste, native options/selection dialogs with focus wrapping/restoration, and same-deck tool returns. Capability map: docs/DECK_BUILDER_WORKSPACE.md. #294 is not merge-approved.
- Same 1366x768 deck baseline: builder moved from y=629 to y=350. Add/Paste each need one direct action instead of opening Actions first. Preserved card art, all views, exact printing/section forms, commitments, settings/delete confirmations, public and League rules, and device-local sandbox state. Initial selector/manifest-guard mistakes were corrected; runtime keyboard, light-theme and 320px/200% reflow issues found during implementation were corrected before delivery.
- Full `npm run verify` at application `808e07c` passed: Prisma generation, typecheck, **577 unit tests**, Windows production build/seven route manifest guards, and **all 49 browser cases** (4.5m, no skips). Includes actual Imports commits, all deck tools, public access, League freezing/printing restrictions, exact trade conservation, vault occupancy and 150,000-copy / 1,200-location / four-owner fixture. Log: `test-results/decks-full-verify.log` (exit 0).
- Final CSS-only polish `82f8fac` narrows the toolbar wrapping rule and gives phone dialog titles a full-width line. Linux production build/seven manifest guards passed; CI run **35753238095** passed. **Both affected Decks browser fixtures passed again** in 15.6s (`decks-final-polish-ui.log`, exit 0). They cover Add card persistence with no inventory creation, empty/public/private decks, all card views, keyboard Tab/Shift+Tab/Escape and opener restoration, safe deletion/return cancellation, selection-only actions, physical return conservation, tool/import returns, 1366/1440/390/320 widths, six themes and 200% text. Full 49 not repeated after this CSS-only polish. Desktop/light/phone/enlarged screenshots inspected locally; captures remain ignored.
- Current cumulative local Docker image **`sha256:cf5ea7aa2a6c7d91b1c0f4a570f189f9c48fae938dffced054ff79642fe0d2d4`**, application `82f8fac`, includes merged #288/#291 plus unmerged #294. Healthy and host login HTTP 200. Review http://127.0.0.1:13001/decks. Base/local/capture-only SMTP overlays retained; no schema change or production deployment.
- Final cleanup: **12,477 physical copies**, zero fixture users, players and uploaded batches. One fixture from an interrupted test-selector probe was explicitly removed by its exact generated identity, then totals were verified after all checks. No build/test process remains active.
- Next safe step: user review of #294; merge only after its individual approval. #268 remains open for library organization and deeper playtest grouping. Known #220/#260/#280 remain open; passing this run does not establish a reliability fix. No outstanding product question, new scheduler or independently verified desktop-project assignment.

## Approved Imports / Locations release and Decks start, 2026-09-22

- User explicitly approved both open PRs #288 and #291, then authorized the recommended Decks builder/navigation batch under #268. Future PRs still require separate approval.
- #288 merged into main as `16387d98389d9a73815dee479793f225122cdb1f`. #291 retargeted to main; merged main into its branch as `0fd2dd9` without application changes. GitHub initially lagged the pushed head; required up-to-date verification is pending. Do not bypass it or enable auto-merge.
- Current branch `feat/imports-workspace`; reviewed local Docker unchanged. Next safe step: finish #291 merge when required checks pass, close resolved issues, branch from updated main for Decks.
- Decks first batch: compact builder identity, optional art/coverage detail, direct Add/Paste, accessible bounded actions/selection, consistent tool navigation. Preserve existing library, all card views, role/League guards and sandbox state; broader library/playtest regrouping remains separate #268 work.


## Imports and Locations ready for review, 2026-09-22

- User authorized #283 followed by Imports #267. Branch `feat/imports-workspace`, PR #291, stacks on `fix/inactive-location-parent` / PR #288 at `bfb84e8`. Both are ready for review, unmerged and not merge-approved. Main remains `5a79fff`.
- #288 is ready: #283 reproduced and fixed; new #289 tracks the native post-save select reset, also fixed. 577 units/core builds passed; full browser run 45/46 exposed #289, followed by final two affected lifecycle passes. CI passed. Local fix image `sha256:f777447368f71b14cc4436ba681f1bb973d647ded0523d600ee78d6961112e49`, healthy/HTTP 200, 12,477 copies and zero fixture users. Full suite not repeated after the final picker correction. Logs `inactive-parent-*`, `parent-reset-*` stay ignored.
- Imports implementation separates CSV, manual add, export and history; batch deep links enter focused review, compact sticky totals link to one destination/commit form, resolver uses a native dialog and retains review query/status. Domain/import mutations and scope checks are preserved.
- Final Imports application commit `3c98f44` includes #267 and fixes #290 (resolver review context), #292 (large-commit placeholder), #293 (light-theme primary contrast). Resolution start/cancel retains review filters. Diagnostics follow the rows; the compact phone summary stays below 270px at 320px/200% text. Saved drafts/unsaved inputs and row-specific placement precedence are explained in the UI.
- Verification: **577 units**, generation/typecheck, Windows/Linux builds and all six manifest guards passed; Imports now checks five client references. Final application CI 35744666046 passed. New fixture covers actual CSV/background local resolution, cancel/commit and copy conservation, row-specific/fallback sections, private/admin boundaries, saved row edits/resolver search/Escape, 120-row review, >=100-row cancelled commit, actual six themes, 1366/1440 desktop, 390/320 phones and enlarged text. Capture/desktop review/light/enlarged screenshots inspected. This is not a production-scale import benchmark or exhaustive contrast certification.
- Browser evidence: full run at `7b07332` had **44 passed / 3 failed**: two test locators needed the new task navigation/accessibility role; unchanged vault-map occupancy assertion reproduced #260 (Sect 0 showed 0 instead of 10). Evidence added to #260 and retained in `imports-final-full-ui.log` / `imports-vault-refresh-context.md`. After layout/context refinements, **all 14 affected cases passed** at `940599c` (`imports-verified-ui.log`). After the final scoped contrast correction, **all five affected cases passed** at `3c98f44` (`imports-contrast-ui.log`). No timeout changes, reload workaround or #260 fix is claimed. Full 47 not repeated after final refinements.
- Current cumulative Docker: **`sha256:96ed4194f8d6f344a43c84a28d8791dc59fd8e5610184de03857e925cdfd3676`**, app `3c98f44`, includes #288/#291; healthy and host login HTTP 200. Review http://127.0.0.1:13001/imports and /locations. Base/local/SMTP-capture overlays preserved; no migrations or production deployment. Core/build logs: `imports-verified-core.log`, `imports-contrast-windows.log`, `imports-contrast-docker.log`.
- Final cleanup verified: zero fixture users/players/uploaded test batches; snapshot still **12,477 physical copies**. Test-owned CSV files removed using validated exact paths. All local build/test processes completed. No outstanding product question.
- Next safe step: user review of #288 and #291, with individual approval required before either merge. Keep #267/#283/#289/#290/#292/#293 open until their fixes merge. Next product candidate is Decks #268; known #220/#260/#280 remain open. No scheduler or independently verified desktop-project assignment. Previous main publication run 35659297784 was reconciled as successful.

## Active inactive-parent fix, 2026-09-22

- User authorized the recommended sequence: fix #283, then Imports #267 as separate reviewable batches. Future PRs require individual merge approval.
- Live main is `5a79fff`; #284/#287 merged and no PR was open at start. Branch `fix/inactive-location-parent`.
- Browser baseline reproduced #283 with a disposable account: description-only save changed the inactive child's saved parent to null. Fixture cleanup completed. Evidence: ignored `test-results/inactive-parent-baseline.log`.
- Fix preserves the saved inactive parent in the editor and allows retained-parent edits only while the existing child stays inactive. Reparenting, owner/system/cycle guards remain. Omitted parent/active service inputs preserve existing values.
- Focused 30 location units pass. Pending: core/build, Docker rebuild, browser regression, cleanup verification, PR. No production work or merge approval.
- Next safe step: validate and publish #283, then consult Imports capability/acceptance notes and implement #267 on a dependent branch. No unanswered product choice.

Updated 2026-09-21. Reconcile git, GitHub, Docker and processes before resuming; GitHub is authoritative.

## Approved Locations release preparation, 2026-09-21

The user explicitly approved all current open PRs (#284 and #287) for main and closure of their resolved issues. This supersedes the unapproved statuses in the historical batch records below; future PRs need new approval.

- #284 merged as `a3ac12d051cc6678c15ddbb9dddfa5a1cde58be6` after passing required Core verification. #287 is retargeted to main; branch `feat/location-storage-configuration` incorporates that merge without changing the reviewed tree. This checkpoint is written before #287's final merge: reconcile GitHub, wait for the refreshed required check, then merge the exact current head. Do not bypass checks or enable auto-merge.
- Release preparation changes only this checkpoint versus reviewed tip `6d3c4b5646a5022cdd8d4f51c8210c723e54091d`; no application/test/dependency/migration changes. Prior 576-unit/build and browser evidence with the disclosed #220 failure remains below; do not claim a fresh full browser run.
- Local Docker remains the reviewed cumulative image `sha256:c677324548f7a1bba82a250df777a97caff850e3ccb3565cc99449bf07cab999`, application `aa45797`, with #284/#287. Preserve base/local/SMTP-capture overlays; no production deployment or data changes are needed for merging.
- Close only resolved issues #266/#285 via #284 and #286 via #287, with resolving links. #283 and reliability #220/#260/#280 are unresolved; broader UI consolidation remains open.
- Final merge/publication evidence belongs in the PR and Foundry Operations entry. After release, reconcile local main and verify Docker health. Next suggested batch is independently reproducing/fixing inactive-parent metadata editing #283, then the Imports workspace #267; no new implementation is part of this merge request.

## Historical feedback batch: configurable storage #286, 2026-09-21

User requested Location types as a peer tab, live search, useful filters (especially space remaining) and guided location creation with optional capacity/default sections. Confirmed reusable type defaults with individually editable location snapshots; capacities remain advisory and unknown capacity is excluded from space remaining. Later type-default changes affect new locations only.

- Branch `feat/location-storage-configuration` from unmerged #284 head `fca46af`. [PR #287](https://github.com/sefaction/MTG-Archives/pull/287) is ready for review, stacked on #284; **neither has merge approval**. Final application/test commit `aa45797`; source CI 35653292484 passed. Earlier feature `ccfab30` and wizard refinement `ac1e6d6` CI also passed. Reconcile any later documentation-only head on GitHub.
- Implemented peer Types tab, debounced URL search, space/type/status filters, Basics / Storage / Review wizard, bounded section generator (collapsed when defaults exist), type defaults/editor, per-location overrides, themed storage maps and shared move/import summaries. Overall-capacity fill shortcut and advisory warnings are supported. Creation/new-type snapshot/audit is transactional. Invalid duplicate-section edits remain inline for correction. See STORAGE_LAYOUTS.md.
- Final **576 unit tests**, generation/typecheck, Windows/Linux production builds and six route manifest guards passed; Locations verifies seven critical client components. Logs `storage-layout-validation-core.log`, `storage-layout-validation-docker.log`. All build/test processes completed.
- Full serial browser run at `ac1e6d6`: **44 passed / 1 failed, zero skips (4.8m)**, log `storage-layout-full-ui.log`. All storage/lifecycle/scale cases passed. Private Scryfall navigation reached the correct URL but rendered only main/alert; the unchanged 10-second Query arguments assertion failed. Evidence added to #220: related symptom, not a proven shared cause. No reliability fix or timeout increase claimed.
- After final inline-validation/unknown-capacity refinement `aa45797`, **six affected cases passed, zero skips (1.1m)**, with no concurrent build/deployment: private/public Scryfall, Locations lifecycle, storage-layout lifecycle and both vault workflows. Log `storage-layout-validation-browser.log`. Full 45 were not repeated after the small refinement. An earlier Scryfall recheck also passed during builds; the final isolated follow-up is the stronger evidence.
- New coverage verifies type creation/default edits, independently persisted overrides, no creation before final confirmation, wizard Back, invalid-name correction without losing drafts, exact old placement preservation, filters without submit, advisory overfill move, overall-capacity fill and manual-add import picker preview (no import submission). Six-theme, 390/320px phone and 200% text checks passed; final desktop/phone/light-theme captures inspected.
- Synthetic scale: 1,200 locations, 3,000 stacks/1,000 printings, four uneven owners (150,000/5,000/500/7 copies). Initial browse DOM 81,727 bytes, 12 filter options, 25 rows/one detail; parent picker at most 32. The 752ms initial request is diagnostic, not a production load benchmark. Bounded rendering does not claim database pagination.
- Current cumulative Docker image **`sha256:c677324548f7a1bba82a250df777a97caff850e3ccb3565cc99449bf07cab999`**, application `aa45797`, includes #284/#287; healthy and host login HTTP 200 verified before final tests. Base/local/SMTP-capture overlays retained. Nullable JSON migration `20260921200000_configurable_storage_layouts` verified applied. No placement backfill. Review http://127.0.0.1:13001/locations.
- Final cleanup: fixture users, players and storage/layout test types all zero; snapshot remains **12,477 physical copies**. Early runs exposed obsolete test selectors and an unintended Continue-to-submit transition (fixed with distinct button identity/default prevention). One early scale run started before host readiness and failed at login; final scale passed after readiness checks. No original collection records removed.
- Next: user review of #284/#287, keeping each PR unmerged until individually approved; then independently reproduce/fix #283 before Imports #267. #220/#260/#280 also remain unresolved. No outstanding product choice, production deployment, scheduling or verified desktop-project assignment.

## Active batch: browse-first Locations #266, 2026-09-21

User asked to keep momentum after the approved Inventory release. Branch `feat/locations-workspace` from merged main `eab4272`; [PR #284](https://github.com/sefaction/MTG-Archives/pull/284) is ready for review, **not merge-approved**. Workspace commit `1bf2a7f`, final application/test refinement `eb7bf2d307769aba223d59c9c1a0df789aedd2ed`. Release publication 35634202217 succeeded. This section supersedes historical pending statuses below.

- Implemented bounded full-path search/list beside selected detail, one full vault map, contextual lazy editor and scoped whole-location move, toolbar Create/shared types, separate bounded read-only deck view. Parent/destination pickers use bounded native search/listboxes. Existing server authorization and mutation services remain; create/move/delete return to scoped browsing. See LOCATIONS_WORKSPACE.md for capability homes and limits.
- Final Windows core passed generation/typecheck, **572 unit tests**, production build and six manifest guards. Final Linux Docker build/guards passed. Logs: `locations-workspace-final-core.log`, `locations-workspace-theme-docker.log`. Source CI 35643901136 passed at `eb7bf2d`; reconcile any later documentation-only head/check on GitHub.
- Complete serial browser run at `1bf2a7f`: **44 passed, zero skips (3.9m)**. After the final theme-only refinement, **7 affected cases passed, zero skips (1.1m)**: hierarchy, scale, Locations lifecycle and both vault workflows. Logs: `locations-workspace-full-ui.log`, `locations-workspace-theme-ui.log`. The full 44 were not repeated after the theme refinement.
- Lifecycle covers create/edit/move/delete, exact section/Back links, deck separation, owner/admin boundaries and owner reset after exiting admin mode. Six-theme reflow/computed label-color assertions, 390/320px phone widths and 200% root font passed. Final stable light/dark screenshots and phone/enlarged layouts were visually inspected. Low-contrast light-theme Locations/shared vault surfaces **#285** are fixed in this PR, pending merge.
- Synthetic scale passed: 1,200 locations, 3,000 stacks across **1,000 printings**, 150,000 copies in the largest owner plus three owners with 5,000/500/7 copies. Browse DOM 80,845 bytes, zero option elements, 25 rows/one detail; selected parent editor at most 32 options. This is bounded rendering coverage, not database pagination or a production load benchmark.
- Final running Docker image: **`sha256:2429bca5aebd6a011e45625c5d277692f123c9a756d414f75e911cf7d892dd12`**, application `eb7bf2d`, healthy with host login HTTP 200. Includes merged Inventory plus unmerged #284. Base/local/SMTP-capture overlays preserved. Review: http://127.0.0.1:13001/locations.
- Fixture users/players and Locations test types are zero; original snapshot remains **12,477 physical copies**. Initial test-label/type-normalization errors were corrected; one precisely identified unused test-owned type was verified unused and removed. All build/test sessions completed. No existing timeout increases, reload workaround or reliability-fix claim.
- Inherited inactive-parent editing risk **#283** remains open (source-confirmed risk, not browser-submitted reproduction): omitted inactive saved parent and backend rejection could detach an inactive child during metadata-only edits. Existing #220/#260/#280 also remain unresolved. No stale-owner state defect reproduced.
- Next safe step: user review of #284; independently reproduce/fix #283 as a separate coherent batch, then Imports #267. No outstanding product choice blocks work. Do not merge #284 without individual user approval; #266/#285 stay open until merge. No production deployment, schema/data migration, scheduling or desktop-project verification.

## Historical release handoff: approved Inventory interface, 2026-09-21

This section supersedes the unapproved/unmerged statuses in the historical batch records below. The user explicitly approved the enumerated four-PR stack (#276, #277, #279, #282) for main. Approval does not extend to future PRs.

- Release scope: design/capability foundation, real-data Inventory, account-backed sidebar/topbar choice, stable left Filters trigger, and Card / Collection / Query tabs with compact color controls.
- Release preparation branch: `feat/inventory-filter-refinement`. #276, #277 and #279 merged in dependency order with required checks intact. #282 is retargeted to main and includes those merges; finish its required verification and merge using its exact current head. Read live GitHub before acting: this checkpoint is written before that final merge. The final merge commit/publication outcome belongs in the PR and Foundry Operations release entry.
- The refreshed release tree matches reviewed tip `37913a17c9a47a797d73c1a2f0819dfc1c932665` except this release checkpoint. No application, test, dependency or migration changes were added during merge preparation. #277 refresh CI 35633304717 and #279 refresh CI 35633676125 passed. Final #282 and main publication checks must be reconciled live.
- Local Docker remains the reviewed cumulative image `sha256:069ad56a61efdac95014df1fbfe9c262f4d45d4e31d65b17f90909326720500f`, application `88ee0fd`; healthy with login HTTP 200 on port 13001. Existing 570-unit/build and 17 focused-browser evidence is detailed below; the full suite was not rerun during documentation-only release preparation. SMTP remains capture-only; no Unraid deployment or production-data operation.
- Close only resolved defects #275 (PR #277), #278 (PR #279), and #281 (PR #282), with resolving links. Broader consolidation packages and reliability #220/#260/#280 remain open.
- Next safe product batch after release: branch from current main for browse-first real Locations #266. Reuse the existing bounded location browser/lazy editors; show search, bounded results and selected counts first, a full map only for the selected vault, contextual creation/edit/moves/types, and a separate deck-managed view. Preserve full paths, exact section links, ownership, visibility and destructive confirmations. No Locations source change has begun; no consequential user choice blocks this authorized batch.
- No unattended schedule or verified desktop-project assignment is implied. Reconcile running publication jobs and Docker before resuming.

## Historical batch: Inventory filter refinement, 2026-09-21

User requested smaller color symbols so Colorless stays on one row and chose tabs to reduce collection-filter scrolling. Branch `feat/inventory-filter-refinement`, [PR #282](https://github.com/sefaction/MTG-Archives/pull/282), stacks on #279 at `372d8cc`; prior final CI 35623082632 passed. Application commit `88ee0fd`. No merge approval for #276/#277/#279/#282.

- Implemented compact private-panel color targets (32px) and symbols, stable scrollbar gutter, explicit per-color keyboard focus. Shared public sizing unchanged. Added six-symbol same-row/target-size/no-overflow browser assertions for desktop and 390/320px phones.
- Existing-image baseline passed in headless Chromium; the user's reported wrap is not universally reproduced in that environment. New sizing targets the reported tight panel layout and scrollbar width.
- User selected **Tabs (recommended)**. Implemented Card / Collection / Query tabs in the same panel, mounted hidden panels preserving drafts, one combined Apply action and all active filters outside. Sticky tab navigation supports arrows/Home/End, restores the current tab per browser session, and opens Query for query errors. Invalid numeric inputs in hidden tabs reveal their owning tab. Public/legacy filters retain their existing layout.
- Cumulative local web is **`sha256:069ad56a61efdac95014df1fbfe9c262f4d45d4e31d65b17f90909326720500f`**, application `88ee0fd`, includes #276/#277/#279/#282. Healthy and login HTTP 200; base/local/SMTP-capture overlays preserved. No schema/domain/mutation changes or real notifications.
- Completed: **570 units**, generation/typecheck, Windows/Linux builds and six manifest guards; application CI passed (35625655509). **17 focused serial browser cases passed, zero skips**: 9 filter/navigation/location cases (52.3s) plus 8 detail/public/export/vault cases (43.8s). Cross-tab quick/Apply composition, retained drafts, arrow navigation, hidden numeric validation, forced Query error reveal, stable filter trigger, six-symbol same-row/24px+ target bounds, 390/320px phones and public-safe behavior verified. Light/dark Card/Collection/Query screenshots inspected; six-theme reflow exercised. No full 43-case or scale rerun for this small batch; prior #279 results remain historical.
- Initial tab build caught synchronous state restoration in an effect; corrected to the project's post-hydration animation-frame pattern. Updated the obsolete source assertion for the Collection and storage heading. Final checks above pass; no disabled checks/timeouts/reload workaround. Logs: `filter-tabs-verified-core.log`, `filter-tabs-final-docker.log`, `filter-tabs-targeted.log`, `filter-tabs-parity.log`; screenshots and earlier baseline/failure logs stay ignored under test-results.
- Final fixture users/players are zero; snapshot remains **12,477 physical copies**. All command sessions completed. #281 is fixed by this PR and stays open until merge; #220/#260/#280 remain unresolved. Durable tab choice recorded in Foundry. Next: user review at `http://127.0.0.1:13001/inventory`, then authorized real Locations #266. No outstanding grouping choice, scheduler, desktop-project verification or production action. Final documentation-only CI should be reconciled on GitHub.

## Previous batch: per-user navigation layout, 2026-09-21

User requested a sidebar/topbar choice and stable Filters positioning. Branch `feat/navigation-layout-preference`, [PR #279](https://github.com/sefaction/MTG-Archives/pull/279), stacks on #277 at `2ef3832`; #276/#277/#279 remain open, unmerged and not merge-approved. Live main remains `7a32dd3`. Application commit `eaace346c840835b6ca78fad452b72fe62e03c3f`; later test/docs correction `ecefba1`.

Scope: account-backed Settings > Appearance choice, sidebar default, shared signed-in Archive shell with responsive topbar; phone Menu and separate anonymous/League navigation preserved. Additive User column; existing settings authentication/audit path retained. User also requested the Inventory Filters trigger stay at the far left when closed/open; fixed with a stable desktop grid and box-position checks in both layouts. No production action.

- Cumulative local web image `sha256:ad2e7171b0222e1fa40e22e4eac950a916c4e4fdf5aaaf75e7a3eb262cd4c63b` includes #276 + #277 + #279 application `eaace34`. Base/local/SMTP-test overlays preserved. Healthy; login HTTP 200, anonymous Inventory correctly returns 307. Additive navigation migration applied, existing accounts default to Sidebar. No production changes or real notification delivery.
- Core verification passed generation/typecheck, **570 units**, Windows/Linux production builds and six manifest guards. CI passed at `eaace34` and test/docs correction `ecefba1` (run 35622432288); final checkpoint-only commit CI should be reconciled on GitHub. Logs `navigation-final-core.log`, `navigation-final-docker.log` (ignored test-results).
- Focused Inventory test passed left-edge/unchanged trigger coordinates, search/selection/phone regressions. Navigation fixture passed explicit Save, fresh-context persistence, second-user isolation, return to Sidebar, 1440/1366/900px reflow, 390/320px keyboard Menu and both-layout filter placement. Six topbar theme screenshots, phone menus and populated sidebar filters inspected. Fixture setup initially hit duplicate player displayName; transaction rolled back and corrected unique names passed. No original collection rows removed.
- First full serial run: **41 passed, 2 failed (4.7m)**. Theme test's obsolete all-radio count updated to count only six theme inputs; not an application defect. Existing deck-analysis test exhausted its unchanged 30s budget; sanitized trace shows a 27.95s deck goto plus slow remote Scryfall images. Tracked separately as **#280**, no fix claimed; private trace preserved ignored as `navigation-deck-timeout-trace.zip`. First full log `navigation-full-ui.log`.
- Final full serial rerun: **43 passed, zero skips (4.2m)**, log `navigation-final-full-ui.log`; no browser/build overlap. Final fixture users/players are zero and snapshot remains **12,477 physical copies**. The existing scale smoke passed with 1,200 locations, 3,000 stacks of one printing and 150,000 copies; not a realistic four-owner/printing-cardinality benchmark. All command sessions completed. PR #279 is locally verified for review, no merge approval. #220/#260/#280 remain unresolved; no timeout increases/reload workaround. #278 tracks the fixed Filters defect, close only after #279 merges.
- Durable choice recorded in Foundry hub/UI plan. Next product batch remains browse-first real Locations #266. No scheduler or desktop project assignment verification.

## Previous batch: sidebar and real Inventory, 2026-09-21

The user accepted the recommended sequence: sidebar + real Inventory, then real Locations, cumulative Docker review. This is implementation authority, not merge approval.

- Branch `feat/inventory-workspace`, application commit `eb565655deaa9309b6186e640fb2c6fa5422bf24`, based on design PR #276 at `2993f20`. [PR #277](https://github.com/sefaction/MTG-Archives/pull/277), base `design/ui-consolidation-foundation`, has completed local verification and is ready for individual review. Required CI passed at the application commit; check the final documentation/test-capture commit's CI on GitHub. Neither PR has merge approval; no merge or production action.
- Scope: grouped signed-in sidebar/phone menu, compact private Inventory, shared quick/structured name draft, side filters and bounded phone dialog, visible criteria and result/copy totals, secondary View options, contextual selection, preserved actions/permissions. Shared summary copy fixes #275. Wider #263/#264/#265/#274 remain open; Locations #266 is next. See INVENTORY_WORKSPACE.md.
- Current cumulative Docker web: `sha256:3c2f00a6ff486d53acf8d54a70874249f4441821b230ca5bf86d0ddb5225cc30`, application `eb56565`, includes design #276 + application #277. Healthy and host HTTP 200. Real app `http://127.0.0.1:13001/inventory`; isolated synthetic prototype still 13002. Base/local/SMTP-test overlays preserved, no production delivery.
- Completed: final generation/typecheck, **569 units**, Windows/Linux production builds and six manifest guards. New owned-fixture browser scenario passed search composition, rendered Back/Forward, selection preservation, view options, desktop Apply visibility, phone focus/Escape, invalid-expression feedback and Account width. Six-theme screenshots and 390/320px layouts inspected; captured theme transitions are now disabled for stable screenshot evidence.
- Full **42-case serial browser run passed, zero skips (4.9 minutes)** against the final image, including the workspace case (7.2s), authentication, public boundaries, trade/League lifecycles and vault moves. The existing scale fixture passed with 1,200 locations, 3,000 stacks of one printing and 150,000 copies; this is not a four-owner realistic-printing-cardinality benchmark. Logs: `inventory-workspace-final-core.log`, `inventory-workspace-final-docker.log`, `inventory-workspace-full-ui.log`; focused scenario `inventory-workspace-fixture.log`. All are ignored under test-results. Processes completed.
- First targeted run reproduced known #260 on the unchanged ten-second Sect 0 count assertion after a reported successful partial move; added evidence on the issue. No fix, reload workaround or increased timeout. #220 remains unresolved too. Passing later runs cannot establish either root cause.
- A new test cleanup initially omitted its automatically created Unassigned location; cleanup is corrected and the one leftover owned account was explicitly removed. After the final suite, fixture users and players are **zero**; snapshot retains **12,477 physical copies**. Disposable fixtures are reproducible; no original collection rows were removed.
- Stable six-theme and phone screenshots were inspected. Real signed-in collection was reviewed read-only. Next safe implementation is browse-first real Locations (#266), retaining #277 cumulatively in Docker; use a separate coherent branch/PR and preserve individual merge approval. No unanswered question blocks it.
- No scheduler; desktop project assignment was not independently reverified. Browser review used only the local signed-in collection, read-only.

## Previous design batch: foundation, 2026-09-21

[PR #276](https://github.com/sefaction/MTG-Archives/pull/276), prototype `5b56661` / checkpoint `2993f20`, remains open and CI passed. It maps all 34 page routes/grouped actions and provides a synthetic Inventory/Locations preview with both navigation alternatives, six themes and 164 targeted checks. The user subsequently selected the recommended sidebar/implementation sequence above. Preview `http://127.0.0.1:13002` runs separately without application/database access. Exact prototype limits remain in docs/design/ui-consolidation. No design PR merge approval.

## Resumption baseline: local availability and roadmap review, 2026-09-21

This section supersedes the historical implementation checkpoint below. The user requested getting the project running and reviewing the current roadmap; no UI implementation batch has begun.

- Branch at resumption: `main`, HEAD `7a32dd3f1f1c11565310cc18366866cc26dc4906`, matching live remote main. The worktree was clean at entry; the resumption initially edited only this checkpoint.
- GitHub authentication is valid. All nine PRs listed in the historical stack below are merged. No open PRs were present at resumption. Final main image publication run `35534041423` succeeded.
- Local services were already running. Web is healthy, host HTTP returned 200, both PostgreSQL services accept connections, and the signed-in home page rendered in Chrome. Pricing/notification workers, Redis and capture-only Mailpit are running. No restart or rebuild was needed.
- Running web image is `sha256:973c4b15dc9d4a1c5b20e43e06d8348791e7046093ca087a1f1bc39cf66a26f2`, matching the exact released image recorded in Foundry Operations. Compose uses the base, local and SMTP-test overlays. App: `http://127.0.0.1:13001`; capture inbox: `http://127.0.0.1:18025`.
- Current roadmap: UI consolidation umbrella #262; foundation/capability map #263 and acceptance ledger #274 first, Inventory/Locations pilot #264-#266 plus copy defect #275 next, then remaining workspaces #267-#273. All #262-#275 are open. Foundry UI Consolidation Plan is the design input; old feature-queue instructions below are historical.
- Independent reliability issues #220 (intermittent filter navigation) and #260 (stale post-move vault occupancy) remain open. No new reproduction or fix is claimed.
- Completed this session: reconcile Foundry hub, roadmap, UI plan, operations and workflow with git/GitHub; verify local availability. Full automated tests, mobile layouts and deep workflows were not rerun. Prior 568-unit/41-browser release evidence remains historical.
- Outstanding choice: review the proposed design-first starting point with the user. Recommended next batch is capability mapping and alternative navigation/Inventory/Locations wireframes under #263/#274, followed by layout review before application implementation.
- No production changes, new feature implementation, merges or recurring schedule. Desktop project assignment was not independently reverified in this session.

## Historical checkpoint from 2026-09-20 (superseded status)

## Authority

Continue the approved audit/feature queue, one PR per coherent batch, cumulative local Docker. **Each PR needs individual user merge approval; no pending PR has it.** Local MTG code, containers and snapshot data are authorized, not production or unrelated projects. Questions block only the affected decision. No recurring scheduler is configured; checkpoints do not automatically restart work.

## Released baseline and review stack

Released main: `e98266fa39cc33023e0b877417c0ced82c5430df`. Prior approved PRs through #236 merged and the published image revision was verified. No Unraid deployment.

Pending stack, in dependency order:

1. #243 `perf/inventory-render-pipeline`, tip `0981089`: reusable collators reduce sort CPU; browser median about 33% lower. Fixes #224; #220 remains observation.
2. #244 `fix/preserve-bootstrap-admin`, tip `fe48d15`: create-only provisioning fixes #242. Concurrent setup and existing-account preservation verified.
3. #247 `test/isolated-backup-restore`, tip `cfba334`: fixes #245/#246, covers #237. Rebuilt-image isolated recovery passed: 48 full table digests plus legacy-price count, 12,477 copies, four roots / 21 entries, malformed-dump guards and real SQL rollback. Disposable targets cleaned.
4. #248 `ci/verification-release-gates`, tip `f4631cd`: portable verification and ordered publishing, covers #241. User-approved main ruleset 23732060 requires Core verification, strict up-to-date, no bypass. Preceding branches gained/passed the check without rewritten history.
5. #250 `test/trade-lifecycle`, tip `782b87f` (app `7233d7c`): fixes #249 provenance loss/#252 duplicate completed history, covers #238. Two-user lifecycle and copy conservation passed.
6. #254 `fix/validated-auth-sessions`, tip `08d287f` (app `cd5c469`): fixes #251. Opaque hashed credentials, expiry/revocation, password binding, rotation, admin reset/edit invalidation and rejection of identity-only legacy cookies. Additive 59th migration; fresh login required. Full 558 units/37 browser cases passed.
7. #255 `fix/local-login-redirects`, tip `9c2f10b` (app `45bf2de`, tests `e53fa28`): fixes #253/#256. Local return-path validation and requested-page preservation across retries; email test/cleanup corrected. Full 561 units/39 browser cases passed.
8. #259 `test/commander-league-lifecycle`, app/test `76f0d8b`: covers #239, fixes #257 malformed-count empty games and #258 stale error after success. Full verification passed below; ready for individual review.
9. #261 `feat/visual-vault-workspace`, app/test `1d3330e`: covers #240. Full verification passed; remains draft because intermittent stale move occupancy #260 is not understood or claimed fixed.

All issues remain open until their resolving PR merges. Passing CI never replaces individual merge approval.

## Previous milestone: League audit complete

Branch `test/commander-league-lifecycle`. The normal lifecycle initially passed against #255. Expanded baselines reproduced a stale validation banner and one empty game created from a nonnumeric count. Both were confined to a disposable league and cleaned. Integer guards and clean success redirect now pass malformed/fractional counts, duplicate rejection and corrected feedback.

Final verification at `76f0d8b`: **563 units**, generation/typecheck, Windows/Linux production builds/six manifest guards, corrected League case (19.8s), and **all 40 serial browser cases with zero skips** (4.3m). Linux core run `35527697072` passed. All command processes completed. Fixture users/leagues verified zero; original inventory remains **12,477 physical copies**.

Previous Docker: `sha256:14cd31c69a66501c5a5378257047c29e297cb606c82a7572e8375f79ce4f34de`, application `76f0d8b`. Logs: `league-docker.log`, `league-corrected-browser.log`, `league-full-verify.log`. Historical local baselines: `league-lifecycle-baseline.log` (test label correction), `league-lifecycle-browser.log` (normal pass), `league-feedback-baseline.log`, `league-count-baseline.log`. See LEAGUE_LIFECYCLE.md for coverage limits.

## Active task: visual vault #240

Branch `feat/visual-vault-workspace`, based on #259 docs tip `83617d4`, app/test commit `1d3330e`, draft PR #261. The map/navigation are integrated in Locations and single-vault Inventory, with exact/empty section filters, advanced-search controls, shared move dialog, five unit cases and an owned-fixture browser lifecycle. Typecheck and all 568 units passed. Initial Docker and original vault pilot passed. New lifecycle initially failed stale occupancy after a successful all-matching move (#260); a focused rerun and five repeats passed. This is not a fix. Keep UI assertion before database diagnostics and timeouts unchanged.

Final current Docker at `1d3330e`: `sha256:563b5245782bc484d84c97ec2a06ff52126adddec6b83e7dfc35ed024af4c46e`, healthy/host HTTP 200. Full cumulative verification PASSED: generation/typecheck, **568 units**, Windows/Linux production builds and six manifest guards, **41 serial browser cases, zero skips** (3.5m). New map lifecycle passed in 5.9s with the UI count assertion before database checks. Linux CI run `35529120610` passed. Final desktop/phone/Locations screenshots inspected. Fixture users and vault locations returned to zero; snapshot still has **12,477 copies**. All command processes completed. Logs: `vault-final-docker.log`, `vault-full-verify.log`; historical failed/retest logs `vault-browser.log`, `vault-map-recheck.log`, `vault-map-repeat.log`. No pending PR has merge approval.

Foundry Inventory Organization was consulted and updated with durable decisions. Six sections remain one physical row, Sect 0Ã¢â‚¬â€œ5 left to right, advisory capacity 85, preserving arbitrary labels/unsectioned cards. See VISUAL_VAULT.md for implementation and coverage. Phone header refinement is included in the current image.

Next safe step: investigate #260 with repeated browser lifecycle runs and credential-free request-completion diagnostics. The first failure was a stale Sect 4 count after a successful 3-copy all-matching move from Sect 10; partial moves refreshed correctly. Inspect server-action revalidation plus the explicit client `router.refresh` without assuming causation. Preserve the original 10-second UI assertion and fixture cleanup. Do not hide it with a reload, longer timeout or database diagnostic before the UI assertion. Keep #261 draft until this observation is adequately resolved or explicitly accepted.

## Remaining queue

- Resolve #260 before completing #240 review readiness, then proportionate parity/UX audits.
- #220 residual inventory navigation observation remains open even when regression tests pass.
- #260 intermittent move occupancy refresh: initial failure, subsequent passes, root cause unproven. Preserve coverage and investigate action/revalidation/refresh behavior; do not mark resolved from passing reruns alone.
- Larger product additions need clarification; connections between installations and broader storage visualization remain roadmap directions.
- No outstanding user question blocks the approved queue. The user chose to keep the current model and continue.

## Local operating reminders

- App http://127.0.0.1:13001; capture-only Mailpit http://127.0.0.1:18025.
- Preserve `docker-compose.yml`, `docker-compose.local.yml`, `docker-compose.smtp-test.yml`. No real snapshot email. One LOCAL global trade announcement webhook remains disabled; tests refuse enabled endpoints.
- After explicit build, use `up -d --no-deps --no-build --pull never web` to avoid the local build policy causing a second build.
- Browser tests serial with `MTG_LOCAL_PILOT_TEST=1`, isolated headless Chromium, not another project's profile. Never overlap browser tests with heavy builds. Confirm host HTTP as well as container health.
- Private logs/traces/fixtures stay ignored under `test-results`; backups under `.local-data/backups`. Never publish credentials, user deck contents, authenticated traces or archives.
- Compatible private recovery capture: `.local-data/backups/drill-e7b779ba-5758-4b40-aa32-b762d9d46bd3`. Older PG18 capture `drill-e171be3b-98c3-403e-87ce-2f3aa99ca6af` is failed evidence, not a verified backup.
- Recovery archive excludes separate pricing DB/configuration/master key. Filesystem copy is not atomic with DB replacement. Revoke restored AuthSession records before exposing a restored web server; see AUTH_SESSIONS.md.
- Read Foundry hub/workflow/relevant notes before batches. Durable knowledge goes there; code/test evidence in repo/GitHub.
